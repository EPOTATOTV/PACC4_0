# PACC 反作弊系统 · 宝塔面板部署教程

> 本教程用 **宝塔面板**（linux 图形化运维面板，网页点点点即可）部署 PACC。
> 比纯命令行版更直观，全程在浏览器里操作，适合习惯用宝塔的朋友。
> 核心仍是 Docker Compose 一条命令拉起整套系统，宝塔只是帮你装 Docker、传文件、放端口。

---

# 第一部分：准备

## 1.1 你需要的东西

- 一台 **云服务器**（建议 Ubuntu 22.04 / Debian 12，2 核 4GB 内存起，见零基础教程第四部分 4.1）
- 一个 **域名**（可选，想用 https 才需要）
- 你的 **项目文件夹**（包含 `docker-compose.yml`、`ptv-backend`、`ptv-frontend`、`deploy`）

## 1.2 宝塔面板是什么？

一个网页版服务器管理工具，相当于给你的服务器装了个"图形桌面"，在上面：

- 装软件（Docker）
- 传文件（项目）
- 改文件（`.env`）
- 开端口（防火墙/安全组）
- 建网站/配 HTTPS 证书

---

# 第二部分：安装宝塔面板

## 2.1 远程登录服务器

本机打开 PowerShell，输入下面命令登录服务器（把 IP 换成你的）回车：

**`ssh root@你的服务器公网IP`**

第一次连会问 `Are you sure you want to continue connecting (yes/no)?`，输入 **yes** 回车，再输密码回车。

## 2.2 安装宝塔面板

在登录后的黑色窗口里，复制粘贴下面这行，回车：

**`curl -sSfL https://download.bt.cn/install/install_6.0.sh | bash`**

> 安装约需 1～3 分钟。装完屏幕会显示：
> - **面板访问网址**（形如 `http://服务器IP:8888/<随机路径>`）
> - **面板账号** 和 **面板密码**

**务必抄下来**（网址、账号、密码）。

## 2.3 打开宝塔面板

在你**自己电脑**的浏览器里，输入刚抄的宝塔网址（`http://服务器IP:8888/...`）回车。

> 打不开？宝塔初始端口 `8888` 要在服务器"安全组/防火墙"放行，见 2.4。

第一次进入会弹"推荐安装"，把默认勾选的 LNMP **全部取消勾选**，点「跳过」——我们不用宝塔自带的 Nginx/MySQL，用 Docker 跑。

**看到这个就是进面板成功**：网页左侧一排菜单（首页、文件、软件商店、网站、安全、Docker 等）。

## 2.4 放行宝塔需要用的端口

云服务器有两道门：**系统防火墙（宝塔安全）** + **云厂商安全组**，都要放。

**宝塔「安全」页**：
1. 左侧点「**安全**」→「**系统防火墙**」
2. 在「放行端口」输入框填入下面这些端口，逐个点「放行」：

```
8888、80、443、8080、8081、3306
```

**云厂商安全组**：
1. 去云厂商控制台找这台服务器 →「安全组/防火墙规则」
2. 添加入方向规则，协议 TCP，放行同样的端口：`8888、80、443、8080、8081、3306`
3. 来源填 `0.0.0.0/0`

> 2898 端口被 8080/8081 等替代？不需要，只放上面列出的即可。3306 仅当需要外部连数据库才开，危险，可先不放。

---

# 第三部分：安装 Docker（宝塔版）

## 3.1 在宝塔「软件商店」装 Docker 管理器

1. 左侧点「**软件商店**」
2. 搜索「**Docker**」，找到并点「**安装**」（通常叫 *Docker 管理器* 或 *Docker 应用商城*）
3. 稍等装完。

> 如果没有这个插件（部分宝塔版本在「Docker 面板」），也可直接用命令行装：
> 回到 SSH 黑色窗口，执行官方脚本 **`curl -fsSL https://get.docker.com | sh`**，再 `sudo usermod -aG docker $USER` 并重连。两种任选其一。

## 3.2 确认 Docker 装好

左侧出现「**Docker**」菜单（或软件商店里能看到 Docker 图标+版本）= 装好了。

> 命令行确认：SSH 里执行 `docker --version`，看到版本号即成功；再 `docker compose version` 确认编排工具。

---

# 第四部分：上传项目并配置

## 4.1 上传项目文件夹

1. 左侧点「**文件**」，进入路径 `/root`（或 `/www`）
2. 点右上角「**上传**」，把整个项目文件夹打成 zip 后上传；或用宝塔「文件」面板的「远程下载」填 Git 地址
3. 上传完成后，右键 zip →「解压」，得到项目文件夹（例如 `/root/pacc`）

**确认看到**：文件夹里有 `docker-compose.yml` 这个文件。

## 4.2 创建配置文件 `.env`

1. 在项目文件夹里找到 `env.example` 文件
2. 右键 →「复制」→ 在空白处右键 →「粘贴」→ 把副本重命名为 **`.env`**
3. 双击 `.env`，宝塔会用编辑器打开

在编辑器里把下面这些行改成自己的值：

```
MYSQL_ROOT_PASSWORD=改成你的数据库超级密码
MYSQL_PASSWORD=改成你的数据库普通密码
PACC_ADMIN_API_KEY=改成你的后台登录密钥  （越长越好）
PACC_SECURITY_SUPER_ADMIN_KEY=改成超级管理员密钥
PACC_SECURITY_JWT_SECRET=改成你的令牌密钥（>=32字符）
PACC_SECURITY_WSS_SIGN_SECRET=改成WSS签名密钥（>=16字符）
```

> ⚠️ 密钥留空或用弱值，后端会拒绝启动（fail-closed），防止带着弱口令上线。
> 懒人生成法：在 SSH 执行 **`openssl rand -hex 48`** 3 次，把输出分别填给上面 3 个密钥（Admin / JWT 各一次 / SuperAdmin / WSS），每次值都不同。

改完点右上角「**保存**」。

---

# 第五部分：启动系统

## 5.1 用「终端」进入项目目录

方法一：宝塔主界面底部有「**终端**」按钮（登录服务器终端），进入后：

```
cd /root/pacc
```

> 注意：**不要用** `cd` 进到别的地方，务必贴到包含 `docker-compose.yml` 的那个文件夹。

## 5.2 一键启动

在终端里输入（复制粘贴），回车：

**`docker compose up -d --build`**

**第一次 10～30 分钟**（在下载软件零件），耐心等，画面停下来光标能输入了就是完成。中途有**大段红色报错**才算问题。

## 5.3 检查状态

输入，回车：

**`docker compose ps`**

看到这几个容器的 **STATUS** 都是 `Up (healthy)` 或 `Up` 即成功：

```
pacc-mysql      Up (healthy)
pacc-backend    Up (healthy)
pacc-frontend   Up (healthy)
pacc-gateway    Up
```

---

# 第六部分：访问 + （可选）绑定域名与 HTTPS

## 6.1 本机浏览器访问

在自己电脑浏览器输入：

**`http://服务器公网IP:8081`**

看到**登录页面**即成功。用 `.env` 里 `PACC_ADMIN_API_KEY` 的值登录进入后台。

## 6.2 一键脚本（可选，省事）

配好 `.env` 后，也可以让脚本自动装 Docker→校验 `.env`→启动→冒烟：

```
chmod +x deploy/server/deploy-server.sh && ./deploy/server/deploy-server.sh
```

## 6.3 绑定域名 + HTTPS（有域名才做，宝塔很方便）

1. 在域名服务商后台，把子域名解析到服务器公网 IP（`admin`、`api`、`pacc`、`dl` + 你的域名）。
2. 在宝塔「**网站**」→「添加站点」里分别添加这些域名的站点（或直接用已解析的域名做反向代理到网关）。
3. 在宝塔「站点」对应域名上点「**SSL**」→ 申请 Let's Encrypt 证书，勾选域名，点「申请」。
4. 把证书文件放到项目 `deploy/gateway/certs/`（`potatotv.asia.crt`、`potatotv.asia.key`），或按 `deploy/gateway/certs/README.md` 操作。
5. 重建网关：`docker compose up -d --build gateway`，就支持 https 访问了。

> 推荐用脚本 `deploy/server/deploy-server.sh --renew` 应用新证书。

---

# 第七部分：常用操作（宝塔界面 vs 命令行）

| 我想做什么 | 宝塔界面 | 命令行（终端里） |
|---|---|---|
| 看容器是否健康 | Docker 页面看容器状态 | `docker compose ps` |
| 重启后端 | Docker → 对应容器 → 重启 | `docker compose restart ptv-backend` |
| 看后端日志 | Docker → 容器 → 日志 | `docker compose logs -f ptv-backend` |
| 停止系统（保留数据） | Docker → 全部停止 | `docker compose down` |
| 改配置 | 文件面板编辑 `.env` | `nano .env` |
| 改完配置生效 | — | 必须 `docker compose up -d --build` |
| 备份数据库 | MySQL 面板导出，或命令行 | `docker exec pacc-mysql sh -c 'exec mysqldump -upacc -p"$MYSQL_PASSWORD" pacc' > backup.sql` |

---

# 第八部分：常见问题

### 1. 宝塔网址打不开 / `8888` 访问不了
- 云厂商安全组需放行 `8888/tcp`
- 宝塔「安全」→ 系统防火墙也放行 `8888`

### 2. `docker compose` 提示找不到命令
- Docker 没装好，回第三部分重装，或在软件商店确认 Docker 已安装
- 若装了 Docker 但没 compose：SSH 执行 `sudo apt install -y docker-compose-plugin`

### 3. 容器 `Exited` 或一直 `restarting`
- 先看日志：`docker compose logs ptv-backend`
- 常见：`.env` 密钥没填全/还是示例值（fail-closed 拒启）、内存不足
- 改过 `.env` 后必须重新 `docker compose up -d --build`

### 4. 端口被占用（`Bind for 0.0.0.0:8080 failed`）
- 关掉占用程序，或改 `.env` 里 `PACC_BACKEND_PORT` 等端口后重新构建

### 5. 网页能开但登录失败
- 确认登录密钥 = `.env` 的 `PACC_ADMIN_API_KEY`（复制粘贴），改过 `.env` 要重建才能生效

### 6. `http://IP:8081` 打不开
- 检查宝塔安全放行 `8081` + 云厂商安全组放行 `8081`
- 确认访问用的是公网 IP

---

*权限、安全组、防火墙是宝塔新手最常见的坑，报「打不开」先看第八部分。*

*命令行纯手把手版见 [部署教程-零基础手把手版.md](部署教程-零基础手把手版.md)。*