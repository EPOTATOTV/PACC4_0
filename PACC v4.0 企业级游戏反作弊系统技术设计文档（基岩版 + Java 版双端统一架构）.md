# PACC v4\.0 企业级游戏反作弊系统技术设计文档（基岩版 \+ Java 版双端统一架构）



**PACC（Professional Anti\-Cheat Client）v4\.0** 是一款面向 Minecraft 基岩版（Bedrock Edition）与 Java 版（Java Edition）双端统一的企业级纯玩家端反作弊系统。反作弊客户端安装于玩家设备，通过内核级驱动与用户态服务采集玩家本地全维度数据；检测数据上报至 **PTV（PotatoTV）管控服务器**进行 AI 深度分析与管理员远程查端。系统**不具备封禁玩家的能力**，检测到作弊时通过**全屏红屏警告 \+ 强制暂停键盘输入 \+ 管理员远程查端**机制响应，并永久记录作弊行为。玩家需**单独注册反作弊账号**，与游戏账号完全解耦。管理面板部署于 PTV 服务器，与游戏服务器无任何关联。

# 系统概述

## 项目背景与架构定位

传统游戏反作弊方案通常依赖游戏服务器插件进行检测与封禁，但在 Minecraft 生态（尤其是基岩版第三方服务器、社区服、Java 版第三方服务器）中存在根本性局限：

- **无法获取游戏服务器数据**：大部分第三方服务器不开放服务器端接口，反作弊系统无法从游戏服务器获取任何玩家数据

- **无法执行封禁操作**：反作弊系统与游戏服务器运营方独立，没有权限踢出或封禁玩家，仅能通过警告、记录与远程查端手段进行威慑

- **底层作弊无法服务端检测**：内存篡改、进程注入、硬件宏、USB 注入器等底层作弊行为完全发生在玩家本地，游戏服务器层面无法观测

- **跨服作弊追踪困难**：作弊者可在不同服务器间切换，单一服务器的记录无法形成全局威慑

- **Java 版与基岩版生态割裂**：Java 版外挂生态（Wurst、Impact、Sigma、Meteor Client 等）与基岩版外挂生态（Horion、Borion、CE 等）差异巨大，需要统一架构覆盖

PACC v4\.0 采用**"纯玩家端检测 \+ PTV 管控 \+ 红屏强制警告 \+ 管理员远程查端"**的双端统一架构，核心定位如下：

**玩家端（唯一数据来源 \+ 检测执行 \+ 红屏执行）**

- 内核驱动级底层数据采集（内存/进程/输入/设备）

- 基岩版 \+ Java 版双端游戏行为数据本地采集

- 轻量本地规则预检测 \+ 端侧 AI 推理

- 红屏警告接收与全屏高优先级显示

- 红屏触发时强制暂停键盘输入

- 被检测玩家锁定等待管理员远程查端

- 反作弊账号独立登录

- 本地证据持久化（重启后保留）

- 不执行任何封禁操作

**PTV 管控服务器（大脑 \+ 记录 \+ 广播 \+ 查端）**

- 接收玩家端上报的检测数据

- AI 深度分析与风险评分

- 作弊行为永久记录与证据留存

- 红屏警告事件生成与全在线广播

- 管理员远程查端通道（远程取证）

- 查端解除限制审批

- 反作弊账号注册/管理体系

- 管理后台部署于此（非游戏服务器）

- 不连接、不获取任何游戏服务器数据

**核心架构约束（不可变更）**：① 系统完全无法封禁玩家，仅能红屏警告 \+ 强制暂停 \+ 远程查端 \+ 永久记录；② 系统完全无法获取游戏服务器的任何数据，所有数据仅来自玩家本地采集；③ 红屏警告采用广播机制，检测到作弊时所有同时在线的已注册反作弊玩家同时收到；④ 红屏触发时强制暂停键盘输入，全屏高优先级覆盖显示；⑤ 被检测玩家停止一切活动，等待管理员远程查端并解除限制；⑥ 检测数据立即本地持久化并上云，玩家重启电脑后数据仍然保留；⑦ 玩家必须单独注册反作弊账号，与游戏账号完全解耦；⑧ 管理面板部署于 PTV 服务器，与游戏服务器无任何关联。

## v4\.0 相对 v3\.0 的核心升级

|升级维度|v3\.0|v4\.0|
|---|---|---|
|游戏端支持|仅基岩版（Bedrock）|基岩版 \+ Java 版双端统一架构|
|红屏响应|全屏红屏显示 5\-10 秒后自动消失|全屏红屏 \+ 强制暂停键盘输入 \+ 高优先级置顶 \+ 未检测者可点继续按钮 \+ 被检测者锁定等待管理员远程查端|
|查端机制|无远程查端|管理员可远程连接被锁定玩家端，进行内存/进程/文件深度取证，查端完成后解除限制|
|数据持久化|证据上传 PTV，本地无强制保留|检测数据立即本地加密持久化（防篡改存储），重启电脑后数据仍然保留，同时上云|
|Java 端外挂|不支持|Wurst、Impact、Sigma、Meteor Client、Future、RusherHack、Pyro、Aristois 等 Java 版主流外挂全量检测|
|Java 端服务器|不支持|进军 Java 版服务器反作弊场景（纯玩家端模式，不依赖服务器插件）|
|端侧 AI|仅规则预检测|端侧轻量 ONNX 模型推理，实时行为异常检测|
|跨平台|Windows/Android/iOS/HarmonyOS|Windows/Android/iOS/iPadOS/HarmonyOS/Linux（Java 版）/macOS（Java 版）|
|编程语言|Java 21 为主|Java 21 LTS 为主语言，全栈语言单独列出（见附录）|

## 设计原则

1. **纯玩家端数据采集**：所有检测数据均来自玩家本地采集，不依赖、不获取任何游戏服务器数据

2. **警告替代封禁**：通过红屏全屏警告 \+ 强制暂停输入 \+ 管理员远程查端 \+ 全在线广播公示 \+ 永久记录形成威慑，不执行任何封禁操作

3. **检测优先，误判可控**：多层级检测 \+ PTV AI 交叉验证 \+ 管理员人工复核，单维度异常不触发红屏锁定，误报率控制在 0\.005% 以下

4. **性能无感**：玩家端 CPU 占用 \< 3%，内存占用 \< 250MB，红屏警告不影响未作弊玩家正常游戏（继续按钮可关闭）

5. **账号独立**：反作弊账号体系完全独立，玩家单独注册登录，与游戏账号无绑定关系

6. **安全自保护**：反作弊客户端自身具备反调试、反注入、反篡改能力，驱动级保护防止被外挂绕过

7. **隐私最小化**：仅采集反作弊必需的本地数据，全链路加密传输与存储，符合 GDPR / 个人信息保护法要求

8. **双端统一**：基岩版与 Java 版共享同一套 PTV 管控架构、账号体系、AI 引擎与管理后台，仅玩家端检测模块差异化

9. **证据不可抵赖**：本地加密持久化 \+ 云端双备份 \+ 哈希链验证，确保作弊证据不可篡改、不可删除、重启后保留

## 信息来源边界说明

本架构明确：**所有数据仅来自玩家端本地采集，系统完全不获取游戏服务器的任何数据**。

|信息类别|数据来源|采集方式|
|---|---|---|
|内存篡改/注入|✅ 玩家本地|内核驱动级内存扫描 \+ 页保护 \+ 特征码匹配（基岩版 C\+\+ 进程 / Java 版 JVM 进程）|
|进程/模块异常|✅ 玩家本地|内核回调 \+ 进程枚举 \+ 模块加载监控 \+ JVM 类加载监控（Java 版）|
|输入设备行为|✅ 玩家本地|IRP 拦截 \+ 点击时序分析 \+ 轨迹运动学特征|
|USB/硬件指纹|✅ 玩家本地|设备枚举 \+ HID 描述符分析 \+ 设备 DNA 画像|
|玩家移动/战斗行为|✅ 玩家本地|游戏内注入模块采集游戏状态与行为数据（基岩版 DLL 注入 / Java 版 Java Agent \+ 字节码注入）|
|资源包/客户端篡改|✅ 玩家本地|本地文件完整性校验 \+ 特征库匹配（基岩版 Addon / Java 版 Mod \& Forge/Fabric 模组）|
|网络协议异常|✅ 玩家本地|本地流量监控 \+ 数据包采集（基岩版 RakNet / Java 版 Netty 协议）|
|Java 版 Mod/外挂|✅ 玩家本地|JVM 类加载检测 \+ 模组清单扫描 \+ 字节码特征匹配 \+ 反射调用监控|
|反作弊账号/在线状态|✅ PTV 管控|玩家端登录 PTV，心跳保活上报在线状态|
|游戏服务器任何数据|❌ 完全不获取|系统与游戏服务器无任何连接、无任何数据交互|

## 系统能力总览

|能力维度|玩家端执行|PTV 管控|
|---|---|---|
|内存篡改检测|驱动级扫描 \+ 页保护 \+ 特征码匹配（双端）|全局特征库 \+ 威胁情报|
|进程/模块监控|内核回调 \+ 注入检测 \+ JVM 类加载监控|黑名单同步 \+ 异常聚类|
|输入行为分析|IRP 拦截 \+ 点击时序 \+ 轨迹运动学|AI 行为画像 \+ 模式识别|
|USB/设备指纹|设备枚举 \+ HID 描述符分析 \+ DNA 画像|设备记录库 \+ 传播链分析|
|游戏行为检测|双端移动/战斗/交互数据采集 \+ 端侧 AI|AI 时序异常检测 \+ 风险评分|
|基岩版特定外挂|CE 专项 \+ 作弊客户端特征库 \+ 幽灵客户端|特征库热更新 \+ 版本适配|
|Java 版特定外挂|JVM 检测 \+ Mod 扫描 \+ 字节码特征 \+ 反射监控|Java 外挂特征库 \+ 版本适配|
|红屏警告|接收广播 \+ 全屏高优先级显示 \+ 强制暂停键盘|警告事件生成 \+ 全在线玩家广播|
|远程查端|被锁定后等待管理员连接 \+ 远程取证通道|管理员远程查端 \+ 证据提取 \+ 解除限制审批|
|作弊记录|本地加密持久化（重启保留）\+ 证据快照上传|永久记录存储 \+ 证据链管理 \+ 记录查询|
|反作弊账号|注册/登录/状态维护|账号体系 \+ 身份管理 \+ 在线玩家管理|
|管理后台|—|数据大盘 \+ 记录管理 \+ 查端控制台（部署于 PTV）|
|封禁/踢出|❌ 不具备此能力|❌ 不具备此能力|

# 整体架构设计

## 双端统一两层分离架构

PACC v4\.0 采用"玩家端检测 → PTV 管控"的两层分离架构，**完全不涉及游戏服务器层**，同时统一支持基岩版与 Java 版：

1. **玩家端检测层**：反作弊客户端安装于玩家设备（Windows/Android/iOS/iPadOS/HarmonyOS/Linux/macOS），以 Windows 为核心平台。通过内核驱动 \+ 用户态服务 \+ 游戏内注入三层防护，实现内存、进程、输入设备、USB 硬件、游戏行为的全维度本地数据采集与轻量预检测。基岩版与 Java 版共享底层检测框架，仅游戏内注入模块与外挂特征库差异化。玩家需使用独立注册的反作弊账号登录。检测到高风险事件时上报 PTV，同时接收 PTV 广播的红屏警告并全屏显示 \+ 强制暂停键盘

2. **PTV 管控层**：部署于 PTV（PotatoTV）独立服务器，是系统的"大脑"。负责反作弊账号注册管理、接收玩家端上报的检测数据、运行 AI 深度分析引擎、生成红屏警告事件并广播给所有同时在线玩家、管理员远程查端、永久记录作弊行为与证据、提供管理后台。**PTV 与游戏服务器无任何连接，不获取任何游戏服务器数据**

**架构数据流**：玩家端本地采集检测数据 → 本地轻量规则 \+ 端侧 AI 预检测 → 高风险/可疑数据上报 PTV → PTV AI 深度分析 \+ 风险评分 → 确认作弊 → ①生成红屏警告事件，广播给所有同时在线的已注册反作弊玩家 ②被检测玩家端强制暂停键盘 \+ 全屏锁定 \+ 等待管理员远程查端 ③永久记录作弊行为 \+ 证据留存（本地 \+ 云端双备份）→ 管理员远程查端取证 → 查端完成后解除限制 → 特征库/策略更新下发玩家端实时生效

## 核心技术栈

|层级|技术选型|说明|
|---|---|---|
|玩家端（Windows）|C/C\+\+、WDF 内核驱动、Java 21 LTS（JNI/FFM 桥接）、Rust（安全敏感模块）|驱动级内存扫描、进程回调、输入拦截、游戏数据采集、红屏显示、远程查端通道|
|玩家端（Android）|Kotlin、C/C\+\+（NDK）、ptrace|Root/非Root双模式检测|
|玩家端（iOS/iPadOS）|Swift、Objective\-C、C（mach\_vm）|越狱/非越狱双模式检测|
|玩家端（HarmonyOS）|ArkTS、C/C\+\+|分布式多端协同检测|
|玩家端（Linux/macOS）|Java 21 LTS、C/C\+\+、eBPF（Linux）|Java 版玩家端支持|
|Java 版注入层|Java 21 LTS、Java Agent、ASM 字节码操作、JVMTI|JVM 类加载监控、字节码注入、反射调用拦截|
|PTV 管控层|Java 21 LTS（Spring Boot 3\.x）、Python（AI）、Go（网关）、MySQL、ClickHouse、Elasticsearch、Redis、Kafka|微服务架构，AI 分析引擎，红屏广播，远程查端，账号体系，管理后台|
|远程查端|WebRTC、gRPC、Java 21 LTS|管理员到玩家端的安全远程取证通道|
|前端管理台|TypeScript、React、ECharts|数据可视化、运营管理、查端控制台（部署于 PTV 服务器）|

## Java 21 LTS 新特性深度应用

v4\.0 全面采用 Java 21 LTS 作为主要开发语言，充分利用新特性提升系统性能与开发效率：

**性能与并发**

- **虚拟线程（Virtual Thread）**：玩家端与 PTV 的大量 IO 密集型任务使用虚拟线程，红屏广播可高效推送到海量在线玩家，远程查端并发连接管理

- **结构化并发（Structured Concurrency）**：简化多任务并行处理的错误处理与取消传播，AI 推理多模型并行

- **外部函数与内存 API（FFM API）**：替代部分 JNI/JNA 功能，玩家端 Java 层与 Native 驱动层交互更安全高效，零拷贝数据传递

- **向量 API（Vector API）**：特征码匹配、内存扫描的 SIMD 加速，Aho\-Corasick 自动机向量化

**开发效率与代码质量**

- **模式匹配（Pattern Matching）**：简化检测事件处理与数据解析代码，switch 模式匹配用于数据包类型分发

- **记录类（Record）**：用于检测数据 DTO、红屏警告事件、查端指令、账号信息等不可变数据

- **密封类（Sealed Classes）**：精确控制检测模块与警告事件继承层次，基岩版/Java 版检测模块分层

- **未命名类与实例 main 方法**：简化工具类与测试入口

- **字符串模板（String Templates）**：安全的日志与查询字符串构建，防注入

## 统一通信协议

全系统采用基于 Protocol Buffers 的统一二进制通信协议，支持 TLS 1\.3 加密传输。**通信仅发生在玩家端与 PTV 之间，与游戏服务器无任何通信**：

|消息类型|方向|说明|
|---|---|---|
|账号注册/登录|玩家端 ↔ PTV|反作弊账号独立注册、登录、身份验证、设备绑定|
|检测上报消息|玩家端 → PTV|玩家标识（反作弊账号）、异常类型、证据快照、时间戳、行为数据、端侧 AI 评分|
|红屏警告广播|PTV → 所有在线玩家|作弊玩家反作弊账号 ID（脱敏）、作弊类型、警告等级、警告时间戳，全屏红屏显示|
|查端指令|PTV → 被锁定玩家|管理员远程查端指令：内存dump、进程列表、文件扫描、注册表查询、网络连接|
|查端数据回传|被锁定玩家 → PTV|远程查端取证数据加密回传，支持大文件分片传输|
|查端解除通知|PTV → 被锁定玩家|管理员查端完成后解除键盘锁定与红屏|
|特征库下发消息|PTV → 玩家端|最新外挂特征码、检测规则、AI 模型参数（基岩版/Java 版分别下发）|
|心跳保活消息|玩家端 ↔ PTV|维持连接，上报在线状态与资源占用，PTV 维护在线玩家列表用于红屏广播|
|配置同步消息|PTV → 玩家端|检测灵敏度、红屏警告策略、查端策略、租户配置统一管理|
|证据补传消息|玩家端 → PTV|离线期间本地缓存的检测数据，联网后自动补传|

# 反作弊账号体系

## 账号独立设计

**核心设计**：反作弊账号体系**完全独立**，玩家必须单独注册反作弊账号才能使用 PACC 反作弊服务。反作弊账号与游戏账号**无任何绑定关系**，系统不获取、不存储、不关联玩家的游戏账号信息（包括 Minecraft 账号、Xbox 账号、Microsoft 账号、Java 版正版账号等）。

## 注册与登录流程

### 账号注册

1. 玩家在 PACC 客户端或 PTV 管理门户提交注册请求（用户名/邮箱 \+ 密码）

2. PTV 账号服务验证用户名/邮箱唯一性，密码使用 Argon2id 加盐哈希存储（v4\.0 升级，替代 bcrypt）

3. 可选：邮箱验证、手机验证、TOTP 二次验证（增强账号安全）

4. 注册成功后生成唯一反作弊账号 ID（UUID 格式），与设备指纹进行初始关联

5. 玩家使用反作弊账号登录客户端，激活反作弊检测服务

### 登录与会话管理

- **登录方式**：反作弊账号 \+ 密码登录，支持"记住登录状态"（本地加密存储 Token，TPM/KeyStore 保护）

- **Token 机制**：登录成功后 PTV 签发 JWT Token（RS256 签名），玩家端后续请求携带 Token 鉴权

- **会话有效期**：默认 7 天，到期需重新登录；支持主动登出与远程登出

- **多设备登录**：同一反作弊账号支持多设备登录，但同一时刻仅一台设备可处于活跃检测状态

- **在线状态维护**：登录后玩家端每 30 秒向 PTV 发送心跳，PTV 维护实时在线玩家列表，用于红屏警告广播

## 账号与设备关联

- **设备指纹绑定**：首次登录时采集设备硬件指纹（CPU/主板/硬盘/网卡/MAC/TPM 等组合），与反作弊账号关联

- **设备 DNA**：USB 设备 DNA 画像与反作弊账号关联，用于识别作弊设备传播链

- **异常登录检测**：新设备登录时触发安全验证（邮箱/手机验证码/TOTP），防止账号被盗用

- **账号与游戏解耦**：系统不记录、不关联玩家的游戏 ID、游戏昵称、游戏服务器等信息，仅通过反作弊账号标识玩家身份

## 账号状态与作弊记录关联

- **信誉评分**：每个反作弊账号维护信誉评分（0\-100），作弊记录会降低信誉评分，查端确认无作弊可恢复

- **作弊记录绑定**：红屏警告事件与作弊证据永久绑定到对应反作弊账号，可在管理后台查询

- **多次作弊标记**：同一反作弊账号多次触发红屏警告时，标记为"高风险账号"，提升检测频率，触发自动查端

- **锁定状态**：被红屏锁定的账号处于"待查端"状态，查端完成前无法使用反作弊服务（但不影响游戏本身）

- **记录公示**：作弊记录可在 PTV 管理后台查询（脱敏展示），形成公开威慑

# 玩家端检测层 — 核心检测模块

## Windows 客户端核心架构

Windows 是 PACC 的核心支持平台，采用经典的三层防护架构，从底层到上层构建纵深防御体系：

|层级|核心组件|核心职责|
|---|---|---|
|内核驱动层|PaccDriver\.sys（WDF 驱动，WHQL/EV 签名）|物理内存直接扫描与保护、系统调用钩子、进程/线程创建回调监控、驱动加载白名单校验、输入设备栈 IRP 拦截、键盘过滤驱动（红屏时强制暂停输入）|
|用户态服务层|PACC Service（SYSTEM 权限，Java 21 LTS 宿主）|与内核驱动 IOCTL 通信、进程全量扫描与特征匹配、系统状态采集与上报、反作弊账号登录维护、红屏警告接收与显示、本地策略执行、远程查端代理、本地证据持久化|
|游戏内注入层（基岩版）|PaccHook\.dll（轻量级注入模块）|游戏内存关键区域完整性校验、游戏内事件回调注册、渲染管线异常检测、本地玩家行为数据采集（移动/战斗/交互）|
|游戏内注入层（Java 版）|PaccAgent\.jar（Java Agent \+ ASM）|JVM 类加载监控、字节码注入采集游戏行为、反射调用拦截、Mod/模组清单扫描、网络包钩子|

## 内存监控模块

内存监控是反作弊系统的核心防线，负责检测所有针对游戏内存的非法读写与篡改行为。此类信息完全来自玩家本地采集。

### 内存页保护机制

通过内核驱动将游戏关键代码段（\.text）和重要数据段设置为只读保护，任何写入操作都会触发页错误（Page Fault），系统在 8ms 内捕获并记录：

- **代码段完整性校验**：计算关键函数区域的 SHA\-256 哈希，周期性比对

- **数据段写保护**：玩家状态、位置、生命值等关键数据区域设为只读

- **页属性监控**：检测是否有程序通过 VirtualProtect/NtProtectVirtualMemory 修改内存页属性

- **Java 版 JVM 内存保护**：监控 JVM 堆内存关键对象（Entity、Player、Movement）的非法修改，通过 JVMTI 字段访问回调检测

### 三级内存扫描策略

采用"全量 \+ 增量 \+ 随机"三级扫描机制，平衡性能与检测率：

|扫描类型|执行频率|检测内容|
|---|---|---|
|全量扫描|每 5 分钟|游戏进程完整虚拟内存空间扫描，特征码全量匹配（基岩版 C\+\+ 进程 / Java 版 JVM 堆 \+ 元空间）|
|增量扫描|每 30 秒|仅扫描内存变化区域，对比前后快照差异，数据量仅为全量的 5\-10%|
|随机扫描|随机间隔 10\-60 秒|随机选择内存区域深度扫描，对抗定时隐藏型作弊|

### 特征码引擎

内置基于 Aho\-Corasick 自动机的多模式匹配特征码引擎，利用 Java 21 向量 API 实现 SIMD 加速，达到 O\(n\) 时间复杂度：

- 已知作弊程序特征码匹配（Aho\-Corasick 算法 \+ SIMD 向量化）

- 通配符特征码支持（如 "8B 45 ?? 89 46 08"）

- 启发式特征：异常代码模式、可疑指令序列

- PTV 特征库热更新，无需客户端重启

- 特征码按风险等级分组，高优先级特征优先匹配

- 基岩版与 Java 版特征库分离管理，按游戏端类型下发

## 进程监控模块

进程监控模块负责检测系统中运行的可疑程序与作弊工具，以及针对游戏进程的注入行为。此类信息完全来自玩家本地采集。

### 进程全量枚举

通过内核层 `PsSetCreateProcessNotifyRoutineEx` 注册进程创建回调，实时监控所有进程启动事件：

- 进程路径、命令行参数、父进程链完整记录

- 进程数字签名验证，拦截未签名可疑程序

- 已知作弊进程黑名单匹配（Cheat Engine、Process Hacker、x64dbg、Wurst 启动器、Impact 加载器等）

- 进程名伪装检测：比对进程名与实际 PE 文件信息

- Java 版专项：检测 javaw\.exe 进程的启动参数（\-javaagent、\-Xbootclasspath、\-agentpath 等异常参数）

### 线程注入检测

监控常见的进程注入技术：

- **远程线程注入**：检测 `CreateRemoteThread`、`NtCreateThreadEx` 调用

- **APC 注入**：监控 `QueueUserAPC` 异常调用

- **反射式 DLL 注入**：检测内存中未映射 PE 映像

- **进程空洞**：检测进程内存与磁盘映像不一致

- **Java 版类注入**：通过 JVMTI 监控 `ClassLoader.defineClass`、`Unsafe.defineAnonymousClass` 的异常调用

### 模块加载监控

通过 `PsSetLoadImageNotifyRoutine` 监控所有模块加载事件：

- DLL 路径、签名、导出表分析

- 检测未签名 DLL 注入到游戏进程

- 检测已知作弊 DLL 特征（Horion\.dll、Borion\.dll、Axon\.dll 等）

- 记录模块加载调用栈，溯源注入来源

- Java 版：监控 JNI 库加载（`System.loadLibrary`），检测原生作弊模块

## 输入设备监控模块

输入设备监控模块负责检测鼠标、键盘等输入设备的异常操作，识别自动点击、连点器、宏脚本等作弊行为。此类信息完全来自玩家本地采集。

### 内核级输入拦截

通过 Minifilter 驱动挂载在 Windows 输入设备栈顶端，拦截所有经由鼠标/键盘设备的 IRP\_MJ\_READ 和 IRP\_MJ\_WRITE 请求：

- **输入源验证**：验证 IRP 来源是否源自 HIDCLASS\.sys 等真实硬件驱动

- **输入数据包结构校验**：检查 HID 报告描述符与数据包签名

- **事件时序特征分析**：分析输入事件间隔误差是否超过人类生理极限

- **红屏键盘锁定**：红屏触发时，键盘过滤驱动丢弃所有键盘 IRP，强制暂停键盘输入（仅 PACC 自身的继续按钮/查端界面可响应）

### 鼠标点击行为分析

针对鼠标左右键点击进行多维度行为分析：

|分析维度|检测方法与阈值|
|---|---|
|点击间隔稳定性|人类点击标准差约 ±150ms，若连续 20 次点击间隔误差 \< 23% 则判定异常|
|点击频率上限|人类单指最快点击约 80\-120ms 间隔，低于 50ms 间隔直接判定为作弊|
|点击时长分布|分析按键按下时长的分布特征，检测是否为固定时长的机械点击|
|操作熵值分析|计算点击序列的信息熵，过低熵值表明非人类操作|

### 鼠标轨迹分析

通过采集鼠标移动轨迹，提取运动学特征：

- **加速度变化**：人类操作有自然的加速/减速过程，宏脚本通常为匀速运动

- **轨迹平滑度**：检测是否存在完美直线或完美圆弧等非人类轨迹

- **反应时间**：从目标出现到鼠标移动的反应时间，人类通常 200ms 以上

- **过冲与修正**：人类瞄准存在过冲和微调修正，作弊器通常精准直达

## USB 设备检测模块

USB 设备检测模块负责识别外接的可疑硬件设备，如硬件宏鼠标、USB 注入器、Arduino 模拟输入设备等物理外挂。此类信息完全来自玩家本地采集。

### 设备枚举与指纹采集

通过 Windows SetupAPI 和 WMI 接口实时监控 USB 设备插拔事件，采集设备指纹信息：

- VID/PID（厂商ID/产品ID）：识别设备品牌与型号

- 设备描述符：产品名称、制造商、序列号

- 接口描述符：HID 接口数量、报告描述符特征

- 设备路径与实例ID：唯一标识物理设备

### 可疑设备识别

建立设备风险评估模型，从多个维度判定设备异常：

|风险维度|检测特征|风险说明|
|---|---|---|
|已知黑名单|VID/PID 匹配作弊设备库|已知硬件宏、USB 注入器等专用作弊设备|
|HID 描述符异常|报告描述符结构异常、多接口复用|Arduino、Teensy 等开发板模拟的 HID 设备|
|设备行为特征|输入精度过高、无自然抖动|设备输出信号过于完美，不符合真实硬件特性|
|厂商信息异常|无厂商信息、通用序列号、虚拟设备|虚拟 HID 设备、未认证的第三方外设|

### 设备 DNA 与外设画像

为每个 USB 设备建立唯一的"设备 DNA"档案，数据上报 PTV 进行跨账号关联分析：

- 基于硬件特征生成设备唯一标识，不受软件修改影响

- 记录设备使用历史、关联反作弊账号、风险评分

- PTV 跨账号设备关联分析，识别作弊设备传播链

- 设备社区信誉系统，用户可举报可疑设备

## 游戏行为数据采集模块

由于系统完全无法获取游戏服务器数据，PACC 在玩家端通过游戏内注入模块直接采集游戏行为数据，上报 PTV 进行 AI 深度分析。基岩版与 Java 版采用不同的注入技术，但采集的数据维度统一。

### 采集内容

**移动行为数据**

- 玩家位置坐标（每秒采样）

- 水平/垂直速度与加速度

- 跳跃频率、跳跃高度、空中停留时间

- 疾跑/潜行/飞行状态切换

- 方块碰撞次数、卡墙频率

- 移动轨迹曲率、方向变化频率

**战斗行为数据**

- 攻击事件（目标、距离、角度、时间戳）

- 每秒攻击次数（CPS）与间隔分布

- 视角旋转速度与瞄准精度

- 目标切换频率与切换模式

- 受击击退距离与方向

- 暴击触发时机与跳跃状态

**交互行为数据**

- 方块破坏/放置事件（类型、速度、频率）

- 物品使用/切换事件（进食、图腾、装备切换）

- 箱子/背包界面操作速度与顺序

- 资源采集路径与矿石分布

- 末影珍珠使用与传送事件

**网络与状态数据**

- 本地网络延迟与丢包率

- 游戏帧率与帧时间分布

- 渲染管线状态异常

- 客户端品牌标识与版本信息

- 资源包/行为包/Mod 加载列表

### 双端注入技术差异

|维度|基岩版（Bedrock）|Java 版（Java Edition）|
|---|---|---|
|注入方式|DLL 注入（CreateRemoteThread / APC）|Java Agent（\-javaagent 参数 / Attach API 动态加载）|
|字节码操作|不适用（原生 C\+\+ 代码）|ASM / ByteBuddy 字节码增强，在关键方法入口/出口插入采集逻辑|
|行为采集点|Hook 游戏原生函数（移动、攻击、渲染）|Transform Minecraft 类（Entity、Player、Minecraft、NetworkManager）|
|网络包采集|Hook RakNet 发送/接收函数|Transform Netty ChannelHandler / NetworkDispatcher|
|Mod 检测|不适用（基岩版无 Mod 生态）|扫描 Mod 清单（mods\.toml / fabric\.mod\.json）、类加载器分析|
|兼容性|需适配每个游戏版本的函数偏移|基于映射表（Mojang Mappings / Yarn）适配，跨版本稳定性更高|

### 采集策略

- **高频实时采集**：移动、战斗等核心行为每秒采样，确保时序完整性

- **事件驱动采集**：攻击、交互等事件发生时即时记录完整上下文

- **本地预检测**：玩家端执行轻量规则检测 \+ 端侧 ONNX 模型推理（速度阈值、CPS上限、行为异常评分），高风险事件立即上报 PTV

- **批量压缩上报**：常规行为数据批量压缩上报，减少网络开销

- **证据快照**：检测到异常时自动保存 5 分钟行为数据回放 \+ 内存快照 \+ 进程列表，加密上传 PTV 永久留存，同时本地加密持久化

## 基岩版特定外挂检测模块

针对 Minecraft 基岩版生态中特有的作弊工具与客户端，PACC 在玩家端提供专项检测能力。

### Cheat Engine \(CE\) 专项检测

Cheat Engine 是基岩版最常用的内存修改工具，PACC 从多个维度进行检测：

**进程与窗口检测**

- 进程名检测：cheatengine\-x86\_64\.exe、cheatengine\-i386\.exe 等

- 窗口标题检测："Cheat Engine"、"Memory Viewer" 等

- 类名检测：CE 窗口类名（如 "Tfrm\_Main"、"Tfrm\_MemoryView"）

- 多语言变体检测：汉化版、修改版 CE 的特征

**驱动与内核检测**

- dbk64\.sys / dbk32\.sys 驱动加载与运行检测

- 驱动名称变体检测：重命名后的 CE 驱动

- 内核回调检测：CE 注册的内核回调函数

- 物理内存读写检测：通过 \\\\\.\\PhysicalMemory 的访问

**内存特征检测**

- CE 注入 DLL 检测

- CE 内存分配特征

- 调试寄存器检测（DR0\-DR7 硬件断点）

- VEH 异常处理检测

### 主流作弊客户端检测

针对基岩版知名作弊客户端建立特征库，实现精准检测：

|客户端名称|检测方式|核心特征|
|---|---|---|
|Horion|内存特征 \+ 模块检测|Horion\.dll、特定字符串特征、导出函数签名|
|Borion|进程注入 \+ 行为特征|Borion 注入模块、ClickGUI 特征、渲染钩子|
|Axon Client|DLL 注入检测|Axon\.dll 特征、DLL 注入路径、配置文件特征|
|FPI / FBI Client|内存特征码|特定功能字节特征、Tab 键菜单特征|
|Undisputed|幽灵客户端检测|无窗口特征、内存隐藏技术、屏幕截图规避|
|Flarial Client|启动器 \+ 模块检测|Flarial Launcher 进程、注入模块特征|
|Toolbox|模组检测 \+ 资源包检测|Toolbox 资源包、行为包特征、修改版客户端|

### 幽灵客户端对抗

针对刻意隐藏自身的"幽灵客户端"（Ghost Client）进行专项检测：

- **内存隐藏检测**：检测通过 VirtualProtect、页面属性修改等方式隐藏的代码

- **线程隐藏检测**：检测从进程线程列表中隐藏的作弊线程

- **截图规避检测**：检测检测到截图时自动关闭功能的作弊行为

- **行为模式识别**：通过 PTV AI 行为分析识别即使无进程特征的幽灵客户端

### 资源包与行为包作弊检测

基岩版 Addon、资源包形式的作弊在玩家端本地检测：

- **行为包检测**：检测修改实体行为、loot 表、配方的作弊行为包

- **资源包检测**：检测 X\-Ray 资源包、透明方块资源包等

- **客户端修改检测**：检测修改版 Minecraft 客户端（如 Toolbox 内置版）

- **脚本 API 滥用检测**：检测利用 Scripting API 的作弊脚本

## Java 版特定外挂检测模块（v4\.0 新增）

**v4\.0 核心新增**：Java 版外挂生态庞大且技术成熟，PACC 通过 Java Agent \+ JVMTI \+ 字节码分析三重手段实现对 Java 版主流外挂的全量检测。所有检测均在玩家本地完成，不依赖游戏服务器。

### Java 版外挂生态概述

Java 版外挂主要分为以下几类：

|类别|代表外挂|技术特征|
|---|---|---|
|通用作弊客户端|Wurst、Impact、Sigma、Meteor Client、Future、RusherHack、Pyro、Aristois、KAMI Blue、Gamesense|以 Forge/Fabric Mod 形式加载，或独立注入器启动，包含 KillAura、Aimbot、X\-Ray 等全功能模块|
|幽灵客户端|Raven B\+、NightX、Skidder、Warez|刻意隐藏自身，无 ClickGUI 或仅通过特定按键呼出，行为特征微弱|
|注入器/加载器|Impact Installer、Sigma Loader、Future Loader、WeepCraft|通过修改启动参数或动态 Attach 注入作弊代码到 Minecraft JVM|
|内存修改器|Cheat Engine（配合 JVM）、GameTrainer、Java 版专用内存修改器|直接修改 JVM 堆内存中的玩家属性（生命值、位置、物品）|
|协议外挂|ViaVersion 滥用、数据包篡改工具、Proxy 作弊|通过本地代理修改客户端\-服务器通信数据包|
|自动化脚本|Baritone（路径自动行走）、AutoFish、AutoTotem 脚本|以 Mod 或外部脚本形式实现自动化操作|

### JVM 层检测

通过 Java Agent \+ JVMTI 在 JVM 层面实现深度检测：

- **类加载监控**：通过 JVMTI `ClassFileLoadHook` 回调监控所有类加载事件，检测作弊客户端的特征类（如 `me/therainbow/wurst/`、`club/minnced/meteor/`、`impact/` 等包名前缀）

- **字节码特征匹配**：对加载的类进行字节码扫描，匹配已知外挂的方法特征、注解特征、字符串常量

- **反射调用监控**：拦截 `Method.invoke()`、`Field.set()` 的异常调用，检测通过反射绕过访问限制的作弊行为

- **Unsafe 操作监控**：监控 `sun.misc.Unsafe` 的内存读写调用，检测直接内存修改

- **Instrumentation 监控**：检测是否有其他 Java Agent 或 `Instrumentation` 实例被加载

- **类加载器分析**：分析自定义 ClassLoader 的层级与加载路径，检测隔离加载的作弊代码

### Mod 与加载器检测

Java 版外挂大多以 Forge/Fabric Mod 形式存在，PACC 从多个维度检测：

- **Mod 清单扫描**：扫描 `mods/` 目录下的 JAR 文件，解析 `mods.toml`（Forge）、`fabric.mod.json`（Fabric）、`mcmod.info`（旧版）清单文件，匹配已知作弊 Mod 的 ID 与名称

- **JAR 内容哈希**：计算 mods 目录下每个 JAR 的 SHA\-256 哈希，与 PTV 外挂哈希库比对

- **加载器类型识别**：识别 Forge、Fabric、Quilt、LiteLoader 等加载器，检测加载器被篡改的情况

- **核心 Mod（CoreMod）检测**：Forge CoreMod 可在加载期修改游戏字节码，是高危作弊载体，重点监控

- **Mixin 配置检测**：扫描 Mixin 配置文件（`*.mixins.json`），检测对关键游戏类的非法注入

### 主流 Java 版外挂专项检测

|外挂名称|检测方式|核心特征|
|---|---|---|
|Wurst|包名 \+ 类特征 \+ Mod清单|net\.wurstclient/ 包名、WurstMod 主类、特征字符串 "Wurst"、Mod ID "wurst"|
|Impact|注入器 \+ 类特征 \+ 网络特征|Impact 启动器进程、club\.ddark/impact 包名、IRC 网络连接特征、Impact 主题色|
|Sigma|加载器 \+ 字节码特征|Sigma Loader 进程、混淆后的动态类加载、Sigma 云验证网络请求|
|Meteor Client|包名 \+ Mod清单 \+ GUI特征|club\.minnced\.meteor 包名、meteor\-client Mod ID、ClickGUI 渲染特征|
|Future|加载器 \+ 内存特征 \+ 网络特征|Future Loader、native 库加载、Future 云验证连接、加密配置文件|
|RusherHack|包名 \+ Mod清单 \+ 行为特征|org\.rusherhack 包名、rusherhack Mod ID、2b2t 专用功能特征|
|Pyro|注入器 \+ 类特征|Pyro 注入器、动态类生成、Pyro 专用模块特征|
|Aristois|加载器 \+ Mod清单 \+ 文件特征|Aristois 加载器、\.minecraft/aristois 目录、配置文件特征|
|Baritone|包名 \+ 行为特征|baritone 包名、路径规划行为特征、自动行走轨迹异常|
|KAMI Blue|包名 \+ Mod清单|me\.zero\.alpine 事件系统、kami 包名、蓝色主题 GUI 特征|

### Java 版行为作弊检测

除了进程/类特征检测，PACC 还通过游戏行为数据采集检测 Java 版特有的行为作弊：

- **KillAura 检测**：攻击角度异常（360度旋转攻击）、攻击距离超限、多目标同时攻击、CPS 异常稳定

- **Aimbot 检测**：视角旋转速度超过人类极限、瞄准精度异常、无过冲修正、目标切换模式机械

- **Reach 检测**：攻击距离超过 3\.0 方块（Java 版正常攻击距离约 3\.0），通过本地坐标精确计算

- **Fly/Speed 检测**：移动速度超过游戏极限、无支撑点的空中移动、加速度不符合重力模型

- **NoFall 检测**：高空落地无伤害、落地前瞬间发送 onGround 数据包

- **Scaffold 检测**：方块放置速度异常、放置角度异常、无需看方块即可放置

- **AutoTotem 检测**：图腾切换速度低于人类反应极限（\< 50ms）、切换时机过于精准

- **Crystal Aura 检测**：末影水晶放置/破坏速度异常、目标选择机械

## 反调试与自保护模块

自保护模块确保反作弊客户端自身不被作弊者绕过、调试或篡改，是整个系统安全的基础。

### 多层反调试机制

**用户态反调试**

- 检测 PEB BeingDebugged 标志

- NtQueryInformationProcess 检测

- CheckRemoteDebuggerPresent 检测

- Java 版：检测 JDWP 调试代理、JVM TI 调试器附加

**内核态反调试**

- 检测内核调试器

- 调试寄存器（DR0\-DR7）检测

- IDT 异常处理检测

**时序与硬件检测**

- 时序反调试：检测单步执行导致的时间异常

- 硬件断点检测：定期检查调试寄存器状态

- TPM 绑定：利用 TPM 2\.0 存储关键密钥，检测运行环境变更

**Hook 检测**

- SSDT / Shadow SSDT Hook 检测

- Inline Hook 检测：关键函数入口字节比对

- IAT / EAT Hook 检测：导入导出表完整性校验

- IRP Hook 检测：驱动设备栈完整性检查

### 进程与线程保护

- **进程保护**：通过内核驱动设置进程保护（PPL / Protected Process Light），禁止 OpenProcess 打开反作弊进程

- **线程保护**：保护反作弊关键线程不被挂起、注入或终止

- **句柄监控**：监控所有打开反作弊进程句柄的操作，记录并拦截可疑访问

- **窗口隐藏**：隐藏反作弊进程的窗口与消息，避免被发现和关闭

- **服务保护**：Windows Service 配置自动恢复，禁止服务被停止/禁用

### 文件与内存完整性

- **文件签名校验**：所有 PACC 组件均经过 EV 代码签名，启动时验证完整性

- **内存完整性校验**：周期性计算自身代码段哈希，检测是否被篡改或 Hook

- **驱动保护**：内核驱动自身防卸载、防停止、防替换（通过注册表保护 \+ 内核回调）

- **配置文件加密**：所有配置文件均加密存储（AES\-256\-GCM），防止篡改

- **本地证据防篡改**：本地持久化的检测数据采用哈希链结构，每条记录包含前一条记录的哈希，任何篡改都会导致链断裂

## 本地证据持久化模块（v4\.0 新增）

**v4\.0 核心新增**：检测数据立即本地加密持久化，玩家重启电脑后数据仍然保留。采用哈希链 \+ 加密存储 \+ 系统级隐藏三重保护，确保作弊证据不可篡改、不可删除、不可抵赖。

### 存储架构

- **存储位置**：系统级隐藏目录（Windows: `%PROGRAMDATA%\PACC\evidence\`，具有系统隐藏 \+ 只读 ACL 属性），普通用户无法查看/删除

- **存储格式**：自定义二进制格式（Protocol Buffers 序列化 \+ AES\-256\-GCM 加密），每条记录包含：时间戳、反作弊账号 ID、检测类型、风险评分、证据数据、前一条记录哈希

- **哈希链结构**：每条记录的 SHA\-256 哈希包含前一条记录的哈希，形成不可篡改的证据链

- **双备份机制**：本地存储 \+ 云端 PTV 存储，任一备份损坏可从另一备份恢复

- **重启保留**：数据存储于非易失性磁盘，重启电脑后完整保留；服务启动时自动校验哈希链完整性

### 数据保留策略

|数据类型|本地保留|说明|
|---|---|---|
|红屏触发证据|永久|红屏警告触发时的完整证据包（行为回放\+内存快照\+进程列表），永久保留|
|可疑行为记录|90 天|风险评分 70\-84 的可疑记录，90 天后自动清理（已上云的除外）|
|常规检测日志|30 天|日常检测明细日志，30 天滚动覆盖|
|查端取证数据|永久|管理员远程查端获取的取证数据，永久保留|
|账号与设备信息|永久|反作弊账号绑定信息、设备指纹、设备 DNA|

### 防删除与防篡改

- **内核级文件保护**：驱动层拦截对证据目录的删除/重命名/修改操作

- **安全模式保护**：即使在安全模式下，证据文件仍受驱动保护（驱动设置为开机启动）

- **哈希链校验**：每次启动校验完整哈希链，发现断裂立即上报 PTV 并标记"证据篡改嫌疑"

- **云端同步**：证据生成后立即加密上传 PTV，本地删除不影响云端记录

- **卸载保护**：卸载 PACC 客户端时，红屏证据文件保留（需管理员权限手动清理），防止作弊者通过卸载销毁证据

## 跨平台客户端

### Android 客户端

- **Root/非Root双模式**：Root 模式下通过 ptrace 读取游戏内存，非Root模式通过无障碍服务\+行为分析检测

- **内存篡改检测**：关键游戏数据定期校验，检测 GameGuardian、八门神器等修改器

- **悬浮窗检测**：检测作弊客户端的悬浮窗（Toolbox、Horion 安卓版等）

- **无障碍服务滥用检测**：检测是否有应用通过无障碍服务模拟点击

- **应用完整性校验**：APK 签名校验、安装来源检测、共存版识别

- **设备指纹**：Android ID、IMEI、MAC 地址、硬件配置组合指纹

- **红屏显示**：通过无障碍服务或 SurfaceView 叠加全屏警告层，强制暂停触摸输入

### iOS/iPadOS 客户端

- **越狱/非越狱双模式**：越狱设备通过 mach\_vm 内存访问检测，非越狱设备通过行为分析\+网络检测

- **作弊客户端识别**：检测侧载应用（TrollStore、AltStore 安装的作弊客户端）

- **手势行为分析**：触控事件时序分析，检测自动点击、脚本操作

- **网络代理检测**：检测 VPN、代理工具（用于绕过检测或注入外挂）

- **设备指纹**：IDFV、设备型号、系统版本组合指纹

- **iPadOS 适配**：支持 iPad 全屏/分屏模式，红屏警告覆盖整个屏幕

- **红屏限制**：非越狱设备无法强制暂停系统输入，采用应用内全屏覆盖 \+ 引导式访问（Guided Access）协同

### HarmonyOS 客户端

- **分布式架构**：利用鸿蒙分布式能力，实现手机/平板/PC 多端协同检测

- **ArkTS 原生适配**：原生鸿蒙应用开发，性能优于兼容层运行

- **原子化服务**：反作弊能力封装为原子化服务，无需安装即可调用

- **硬件级安全**：利用鸿蒙 TEE 可信执行环境保护检测逻辑

### Linux/macOS 客户端（v4\.0 新增，Java 版专用）

- **Linux 支持**：通过 eBPF 进行系统调用监控、进程追踪、网络包捕获；Java Agent 注入 Minecraft JVM

- **macOS 支持**：通过 Endpoint Security Framework 监控进程/文件事件；Java Agent 注入

- **红屏显示**：Linux 通过全屏覆盖窗口（X11/Wayland），macOS 通过 NSScreen 全屏窗口

- **输入锁定**：Linux 通过 evdev 抓取键盘设备，macOS 通过 CGEventTap 拦截事件

- **适用场景**：主要面向 Java 版玩家（基岩版在 Linux/macOS 上份额较低）

# 红屏警告系统（v4\.0 升级版）

## 系统定位与设计理念

**核心定位**：红屏警告是 PACC 系统**唯一的作弊响应手段**。系统完全无法封禁或踢出玩家，通过**全屏红屏警告 \+ 强制暂停键盘输入 \+ 高优先级置顶显示 \+ 管理员远程查端 \+ 全在线广播公示 \+ 永久记录**形成公开威慑与强制取证机制。红屏警告的设计参考 ACE（Anti\-Cheat Expert）等反作弊系统的警告机制，但采用**全在线广播 \+ 被检测者锁定待查**的增强模式。

## 红屏警告触发机制

### 触发条件

红屏警告并非每次检测到异常就触发，而是经过多层验证确保准确性：

1. **玩家端本地预检测**：玩家端检测到高风险异常（如内存特征码命中、CE 进程运行、点击间隔异常、Java 版 Wurst 类加载等），立即上报 PTV

2. **PTV AI 深度分析**：PTV 接收上报数据后，运行 XGBoost 分类模型 \+ LSTM\-AE 时序异常检测 \+ 端侧 AI 评分交叉验证，综合计算风险评分

3. **多维度交叉验证**：单一维度异常不触发红屏，需至少 2 个独立检测维度同时异常（如内存篡改 \+ 行为异常，或进程注入 \+ 输入异常，或 Java 版 Mod 特征 \+ KillAura 行为）

4. **风险评分阈值**：综合风险评分 ≥ 85 分时触发红屏警告（可配置）

5. **冷却期机制**：同一反作弊账号触发红屏后有 10 分钟冷却期，期间不重复触发相同类型警告，避免刷屏

### 警告等级与响应

|等级|风险评分|红屏显示效果|记录与查端处理|
|---|---|---|---|
|一级警告（可疑）|70\-84|不触发红屏，仅 PTV 记录，提升该账号检测频率|记录为"可疑行为"|
|二级警告（确认作弊）|85\-94|全屏红屏 \+ 警告文字 \+ 强制暂停键盘，被检测者锁定待查端，未检测者显示继续按钮|记录为"作弊行为"，永久留存证据，触发管理员查端队列|
|三级警告（严重作弊）|95\-100|全屏红屏 \+ 闪烁 \+ 警告文字 \+ 作弊类型 \+ 强制暂停键盘，被检测者锁定待查端，未检测者显示继续按钮|记录为"严重作弊"，标记高风险账号，永久留存证据，优先查端队列|

## 红屏显示与输入锁定机制（v4\.0 核心升级）

### 全屏高优先级显示

- **全屏红色 overlay**：在游戏画面上层渲染不透明红色全屏遮罩（透明度 100%），完全覆盖显示屏页面

- **高优先级置顶**：通过 `WS_EX_TOPMOST` \+ `SetWindowPos(HWND_TOPMOST)` \+ 内核级窗口管理，确保红屏窗口始终位于最顶层，即使游戏使用全屏独占模式也能覆盖

- **多显示器支持**：红屏警告同时覆盖所有连接的显示器，防止作弊者将游戏移至副屏规避

- **警告文字**：屏幕中央显示大号白色警告文字，内容包括："PACC 检测到作弊行为"、作弊类型（如"内存修改 / 自动点击 / 作弊客户端 / Java版 Wurst"）、作弊者账号（脱敏）、时间

- **二级警告**：静态红屏 \+ 文字，持续显示直到查端完成

- **三级警告**：红屏闪烁（1Hz 频率）\+ 文字 \+ 边框高亮，持续显示直到查端完成

### 强制暂停键盘输入

**核心机制**：红屏触发时，内核键盘过滤驱动（PaccKbdFilter\.sys）立即进入"锁定模式"，丢弃所有键盘 IRP 请求，**强制暂停键盘输入**。鼠标输入可保留用于点击继续按钮（未检测者）或查端界面操作（被检测者），但键盘完全锁定直到管理员远程查端完成并解除限制。

- **内核级键盘过滤**：通过 WDF 过滤驱动挂载在键盘设备栈（Kbdclass / i8042prt / HID Keyboard）之上，红屏时丢弃所有 IRP\_MJ\_READ / IRP\_MJ\_WRITE

- **全键盘锁定**：包括普通按键、功能键、Win 键、Ctrl\+Alt\+Del（通过 SAS 禁用）、快捷键组合，确保作弊者无法通过键盘关闭反作弊或切换窗口

- **鼠标保留**：鼠标输入不锁定，未检测者可点击"继续"按钮关闭红屏；被检测者鼠标可用于查端界面交互

- **解锁条件**：仅当 PTV 管理员完成远程查端并发送"解除限制"指令后，键盘过滤驱动恢复正常模式

- **重启保留锁定**：被检测者重启电脑后，PACC 服务启动时检测到"待查端"状态，自动重新进入红屏锁定模式，键盘仍然暂停

### 未检测玩家的继续按钮

对于**未被检测为作弊者**的在线玩家（收到广播红屏但本人不是作弊者），红屏显示如下交互：

- **红屏广播显示**：全屏红屏 \+ 警告文字（"检测到玩家 \[脱敏ID\] 使用作弊：\[作弊类型\]"），公示作弊行为

- **继续按钮**：屏幕下方显示"我已知晓，继续游戏"按钮，玩家点击后红屏消失，恢复正常游戏

- **自动消失**：若玩家未点击，红屏持续显示 10 秒后自动消失（未检测者不锁定键盘）

- **键盘不锁定**：未检测者的键盘输入正常，仅红屏 overlay 覆盖显示

- **公示效果**：所有同时在线玩家同时看到作弊公示，形成社区监督与公开威慑

### 被检测玩家的锁定待查端状态

对于**被检测为作弊者**的玩家，红屏显示如下锁定状态：

- **全屏红屏锁定**：全屏红屏 \+ 警告文字（"您的账号已被检测到作弊行为，系统已暂停您的键盘输入，请等待管理员远程查端"），无继续按钮

- **键盘强制暂停**：内核键盘过滤驱动锁定，所有键盘输入无效

- **停止一切活动**：玩家无法进行任何键盘操作，游戏内角色无法移动/攻击/交互（但游戏本身仍在运行，PACC 不关闭游戏）

- **查端倒计时**：屏幕显示"正在等待管理员查端，预计等待时间：X 分钟"，并显示查端队列位置

- **数据立即记录**：检测触发瞬间，所有本地数据（内存快照、进程列表、行为回放、设备信息）立即加密持久化到本地 \+ 上传 PTV 云端

- **重启后保留**：玩家重启电脑后，PACC 服务自动检测到"待查端"状态，重新进入红屏锁定 \+ 键盘暂停，数据仍然保留

- **解除条件**：仅管理员远程查端完成后，发送解除指令，红屏消失、键盘恢复

## 全在线广播机制

**核心设计**：红屏警告采用**全在线广播**模式——当 PTV 确认某玩家作弊时，**所有同时在线的已注册反作弊玩家同时收到红屏警告**，而非仅作弊者本人可见。这确保作弊行为被所有正在使用反作弊的玩家目击，形成公开威慑与社区监督效果。被检测者额外进入锁定待查端状态。

### 广播流程

1. **PTV 生成警告事件**：AI 分析确认作弊后，PTV 生成红屏警告事件，包含：警告等级、作弊类型（脱敏描述）、作弊者反作弊账号 ID、时间戳、是否锁定待查端

2. **获取在线玩家列表**：PTV 从心跳维护的实时在线玩家列表中，获取所有当前在线的已注册反作弊玩家连接

3. **并行广播推送**：PTV 通过 WebSocket 长连接，并行向所有在线玩家推送红屏警告事件。利用 Java 21 虚拟线程实现高并发推送，万级玩家同时推送延迟 \< 1 秒

4. **玩家端接收显示**：每个玩家端接收到广播事件后，立即在游戏画面上层渲染全屏红屏警告 overlay。被检测者额外触发键盘锁定 \+ 待查端状态

5. **广播结果回执**：玩家端显示完成后向 PTV 发送回执，PTV 记录广播送达率与显示成功率

### 广播技术保障

- **长连接维护**：玩家端与 PTV 维持 WebSocket 长连接，心跳间隔 30 秒，断线自动重连（指数退避）

- **高并发推送**：PTV 采用 Java 21 虚拟线程 \+ Netty 事件驱动，支持 10 万\+ 在线玩家同时广播

- **消息可靠性**：红屏警告事件带唯一 ID \+ 序号，玩家端去重处理；推送失败自动重试 3 次

- **离线补偿**：推送时不在线的玩家不补发（红屏警告具有时效性，仅对同时在线玩家可见）

- **广播延迟目标**：从 PTV 生成事件到 99% 在线玩家显示红屏，延迟 \< 3 秒

## 管理员远程查端系统（v4\.0 核心新增）

**v4\.0 核心新增**：被检测玩家锁定后，管理员可通过 PTV 管理后台发起**远程查端**，安全连接到玩家端进行深度取证。查端完成后管理员可解除限制（确认误报）或维持记录（确认作弊）。查端全程录像留痕，确保流程可审计。

### 查端流程

1. **查端请求**：管理员在 PTV 管理后台的"待查端队列"中选择被锁定玩家，发起远程查端请求

2. **安全通道建立**：PTV 向玩家端发送查端指令，建立 WebRTC \+ DTLS\-SRTP 加密点对点通道（或通过 PTV 中继），管理员身份通过 mTLS 双向认证

3. **远程取证**：管理员通过查端控制台执行以下操作（所有操作记录审计日志）：  

    - 内存 dump：导出游戏进程完整内存镜像

    - 进程列表：获取全量进程、模块、线程信息

    - 文件扫描：扫描游戏目录、mods 目录、常见作弊路径

    - 注册表查询：查询启动项、驱动注册、服务配置

    - 网络连接：获取活跃网络连接、监听端口

    - 屏幕截图：实时查看玩家屏幕（仅查端期间）

    - 行为回放：查看触发前 5 分钟的行为数据回放

4. **查端结论**：管理员根据取证结果给出结论：  

    - **确认作弊**：维持红屏记录，标记证据，解除键盘锁定（但记录永久保留）

    - **误报**：撤销红屏记录，恢复信誉评分，解除锁定

    - **待进一步分析**：维持锁定，将证据转交高级分析团队

5. **解除限制**：管理员发送"解除限制"指令，玩家端键盘过滤驱动恢复正常，红屏消失

### 查端安全与权限

- **权限分级**：查端操作需管理员角色（L2 以上），高级取证（内存 dump、文件系统访问）需 L3 管理员审批

- **全程审计**：查端过程的所有操作、命令、数据传输均记录审计日志，不可删除

- **玩家告知**：查端开始时红屏显示"管理员正在远程查端，请配合"，玩家知晓查端正在进行

- **数据最小化**：查端仅采集与作弊检测相关的数据，不访问个人文件、聊天记录等隐私信息

- **超时机制**：单次查端最长 30 分钟，超时自动断开；查端队列中等待超过 2 小时的自动降级为"记录留存，不锁定"

- **断网处理**：查端过程中玩家断网，锁定状态保留，联网后自动重新进入待查端队列

## 作弊记录与证据留存

每次红屏警告触发时，系统永久记录作弊行为并留存完整证据链：

- **记录内容**：反作弊账号 ID（脱敏）、警告等级、作弊类型、触发时间、风险评分、检测维度明细、查端结论

- **证据留存**：玩家端自动采集触发前 5 分钟 \+ 触发时的行为数据回放、内存快照、进程列表、检测日志，加密上传 PTV 永久存储，同时本地加密持久化（重启保留）

- **记录查询**：PTV 管理后台支持按账号、时间、作弊类型查询作弊记录，查看证据详情与查端记录

- **记录公示**：作弊记录可在 PTV 管理门户脱敏公示（仅显示账号脱敏 ID \+ 作弊类型 \+ 时间），形成公开记录库

- **多次作弊累计**：同一反作弊账号的作弊记录累计，多次触发红屏警告后标记为"高风险账号"，在管理后台高亮显示，触发自动优先查端

- **证据哈希链**：每条证据记录包含前一条记录的 SHA\-256 哈希，形成不可篡改的证据链

# PTV 管控层 — 数据处理与管理平台

## 平台架构

PTV 管控层是整个系统的"大脑"，部署于 PTV（PotatoTV）独立服务器。**PTV 与游戏服务器无任何连接，不获取任何游戏服务器数据**，仅与玩家端反作弊客户端通信。采用微服务架构：

|服务名称|核心职责|
|---|---|
|API 网关|统一入口，鉴权、限流、路由，接收玩家端连接（WebSocket \+ HTTPS）|
|账号服务|反作弊账号注册、登录、身份验证、会话管理、设备绑定、信誉评分|
|检测数据服务|接收玩家端上报的检测数据与行为数据，写入时序数据库，数据清洗与标准化|
|AI 分析服务|模型训练、批量推理、异常玩家聚类、行为画像构建、风险评分、双端模型分别管理|
|红屏警告服务|警告事件生成、全在线玩家广播推送、广播状态管理、锁定状态维护|
|远程查端服务|查端队列管理、WebRTC 信令、查端会话管理、取证数据接收、查端结论审批|
|记录服务|作弊记录永久存储、证据链管理、记录查询与公示、哈希链验证|
|特征库服务|管理外挂特征码、检测规则、AI 模型参数，支持热更新下发到玩家端（基岩版/Java 版分别管理）|
|在线状态服务|维护玩家端心跳、实时在线玩家列表、用于红屏广播|
|运营后台服务|管理后台 API，数据统计，报表生成，查端控制台（部署于 PTV，非游戏服务器）|

## AI 智能检测引擎

PTV 管控层运行核心 AI 分析引擎，对玩家端上报的海量检测数据与行为数据进行深度分析。

### 四层 AI 检测架构

|层级|名称|技术手段|运行位置|
|---|---|---|---|
|L1|规则引擎层|基于明确阈值的快速检测，覆盖 80% 常见外挂|玩家端（轻量预检测）\+ PTV|
|L2|统计分析层|基于滑动窗口的统计特征检测，识别伪装较好的外挂|PTV|
|L3|机器学习层|基于 XGBoost、随机森林的行为分类，基岩版/Java 版分别训练|PTV（实时推理）\+ 玩家端（ONNX 端侧推理）|
|L4|深度学习层|基于 LSTM/Transformer 的时序行为分析与异常检测|PTV（批量推理）|

### 特征工程体系

PTV 基于玩家端上报的本地行为数据，构建多维度特征体系（基岩版与 Java 版共享特征框架，阈值差异化）：

**战斗行为特征（52 维）**

- 攻击统计：CPS均值/方差/最大值、攻击间隔分布、命中/未命中比例（12维）

- 瞄准特征：视角旋转速度/加速度、瞄准误差、目标切换频率（16维）

- 距离特征：攻击距离均值/方差/最大值、距离分布直方图（10维）

- 反应特征：目标出现到攻击的反应时间、反应时间分布（8维）

- 连击特征：最大连击数、连击间隔、连击命中率（6维）

**移动行为特征（44 维）**

- 速度特征：水平/垂直速度均值/方差/最大值、加速度分布（12维）

- 轨迹特征：移动轨迹曲率、方向变化频率、路径直线度（10维）

- 跳跃特征：跳跃频率、跳跃高度、空中停留时间、跳跃节奏（8维）

- 碰撞特征：方块碰撞次数、穿墙次数、卡墙频率（6维）

- 状态特征：疾跑/潜行/飞行状态切换频率、状态持续时间分布（8维）

**环境与设备特征（30\+ 维）**

- 进程异常：可疑进程数、注入事件数、未签名模块数

- 内存异常：页错误次数、特征码命中数、内存修改频率

- 输入异常：点击间隔标准差、操作熵值、轨迹平滑度

- 设备异常：USB 设备风险评分、HID 描述符异常数

- 网络异常：延迟抖动、丢包率、异常数据包频率

- Java 版专属：Mod 数量、异常类加载数、反射调用频率

### 机器学习模型

#### 实时分类模型

- **模型类型**：XGBoost 二分类模型（正常/作弊），基岩版与 Java 版分别训练独立模型

- **输入特征**：上述 126\+ 维行为特征，滑动窗口 30 秒

- **推理性能**：单样本推理 \< 5ms，支持每秒 10000\+ 玩家并发检测

- **模型更新**：每周使用新标注数据重新训练，灰度发布

- **准确率**：AUC \> 0\.98，精确率 \> 0\.95，召回率 \> 0\.90

#### 时序异常检测模型（零样本能力）

- **模型类型**：LSTM\-AE 自编码器异常检测

- **输入**：玩家 5 分钟内的行为时序数据（每秒采样，来自玩家端本地采集）

- **原理**：学习正常玩家的行为模式，重构误差超过阈值则判定异常

- **优势**：无需作弊样本即可检测新型外挂，零样本检测能力

- **推理**：PTV 批量推理，每 5 分钟对在线玩家全量扫描

#### 玩家行为画像模型

- **模型类型**：用户嵌入（User Embedding）\+ 聚类分析

- **输入**：反作弊账号长期行为统计特征（7天/30天）

- **输出**：128 维玩家行为向量，用于相似度计算与异常玩家聚类

- **应用**：识别作弊团伙、账号交易、代练行为、设备传播链

## 风险评分与红屏触发

### 多维度风险评分

PTV 为每个反作弊账号维护一个实时风险评分（0\-100分），由以下维度加权计算：

|评分维度|权重|说明|
|---|---|---|
|底层检测分（内存/进程/设备）|35%|玩家端上报的内存篡改、进程注入、USB 设备异常等底层检测结果|
|行为检测分（移动/战斗/交互）|25%|玩家端采集的游戏行为数据，经 PTV AI 分析后的异常评分|
|历史行为分|20%|最近 7 天的异常记录、红屏警告记录、举报记录、查端结论|
|AI 模型分|15%|XGBoost \+ LSTM\-AE 模型输出的作弊概率|
|环境风险分|5%|客户端环境异常（调试器、注入模块、虚拟机、Java 版异常 Mod 等）|

### 红屏触发决策

|风险评分|响应动作|说明|
|---|---|---|
|0\-50|正常记录|无警告，正常记录检测数据|
|51\-69|关注 \+ 提升检测频率|PTV 记录，提升该账号玩家端检测采样频率|
|70\-84|可疑记录（一级）|PTV 记录为"可疑行为"，不触发红屏，持续重点监控|
|85\-94|红屏警告（二级）|PTV 生成红屏警告事件，广播所有在线玩家，被检测者全屏锁定 \+ 键盘暂停 \+ 待查端，永久记录|
|95\-100|红屏警告（三级）|PTV 生成严重红屏警告，广播所有在线玩家，闪烁红屏 \+ 锁定 \+ 键盘暂停 \+ 优先查端，永久记录，标记高风险账号|

**重要**：系统在任何风险评分下都**不会执行封禁或踢出操作**。最高响应为三级红屏警告 \+ 键盘锁定 \+ 远程查端 \+ 永久记录 \+ 高风险标记。系统完全没有能力将玩家从游戏中移除。

## 特征库管理与下发

- **特征码管理**：PTV 管理后台支持外挂特征码增删改查，按风险等级分组，基岩版/Java 版分别管理

- **检测规则配置**：支持动态调整各检测模块的灵敏度与阈值

- **灰度发布**：新特征库/规则按 1% → 10% → 50% → 100% 逐步推送到玩家端，监控效果

- **版本回滚**：特征库支持多版本并存，出现问题可快速切换回稳定版本

- **秒级下发**：特征库更新后 10 秒内全量玩家端生效

- **AI 模型下发**：端侧轻量 ONNX 模型参数增量下发，无需客户端完整更新

## 数据存储架构

|数据类型|存储方案|保留时间|
|---|---|---|
|反作弊账号信息|MySQL 8\.0|永久|
|检测日志（明细）|ClickHouse|90天|
|行为特征数据|ClickHouse|30天|
|红屏警告记录|MySQL 8\.0|永久|
|查端记录与取证|MySQL \+ 对象存储|永久|
|作弊证据（快照/回放）|对象存储（S3兼容）|永久（红屏触发的证据）|
|特征库/规则|MySQL \+ Redis 缓存|永久\+版本历史|
|系统日志|Elasticsearch|30天|
|AI 模型|对象存储 \+ 模型仓库|永久（版本化）|
|审计日志|Elasticsearch \+ 冷存储|永久|

## 管理后台（部署于 PTV 服务器）

**重要声明**：管理后台**完全部署于 PTV 管控服务器**，**不在游戏服务器中**，与游戏服务器无任何关联。游戏服务器不运行任何 PACC 管理组件，不存储任何 PACC 数据。

管理后台功能模块：

- **数据大盘**：实时在线反作弊账号数、检测拦截数、红屏警告次数、作弊类型分布、趋势图表、玩家端在线率、广播送达率、查端队列状态

- **账号管理**：反作弊账号详情页、行为画像、历史记录、风险评分趋势、信誉评分、设备关联分析、高风险账号标记

- **红屏警告管理**：警告记录列表、按等级/类型/时间筛选、警告详情查看、广播送达统计、证据回放（行为录像\+内存快照\+检测日志）

- **远程查端控制台**：待查端队列、查端会话发起、远程取证操作界面、查端结论录入、查端历史记录、查端审计日志

- **作弊记录管理**：永久记录查询、记录公示管理、证据链管理、多次作弊账号统计、误报撤销

- **检测日志**：多维度筛选、详情查看、证据下载、导出报表

- **特征库管理**：特征码增删改查、规则配置、灰度发布、版本回滚、AI 模型管理（基岩版/Java 版分别管理）

- **设备管理**：USB 设备 DNA 库、设备风险记录、设备传播链分析、可疑设备举报处理

- **系统设置**：管理员账号与权限、操作审计、告警配置、红屏警告策略配置（阈值/持续时长/广播范围）、查端策略配置、系统参数

## 开放 API

提供 RESTful API 供第三方系统集成（仅与 PTV 交互，不涉及游戏服务器）：

- **账号查询 API**：查询反作弊账号风险评分、红屏警告记录、历史作弊记录、查端结论

- **检测数据上报 API**：自定义检测数据上报，扩展检测能力

- **Webhook 回调**：红屏警告事件、查端完成事件实时推送到第三方系统

- **统计数据 API**：获取检测统计、作弊趋势数据、红屏警告统计

# 部署与集成方案

## 玩家端客户端部署

**Windows 客户端**

- Inno Setup 安装包，一键安装驱动、注册服务、创建快捷方式

- 支持单文件绿色版（Enigma Virtual Box 打包）

- 驱动需 WHQL/EV 数字签名，正式分发前完成微软硬件兼容性认证

- 支持静默安装，便于网吧、电竞馆批量部署

- 安装后启动客户端，使用反作弊账号登录，自动连接 PTV 同步特征库

- 支持基岩版与 Java 版自动识别，按需加载对应注入模块

**Android 客户端**

- APK 安装包，支持 Root/非Root双模式自动切换

- 可集成到游戏启动器中，随游戏启动

- 支持应用内更新，无需重新下载 APK

**iOS/iPadOS 客户端**

- 非越狱设备通过 TestFlight 分发

- 越狱设备通过 Cydia/Sileo 源分发

- 支持侧载安装（TrollStore 等）

**HarmonyOS 客户端**

- 鸿蒙应用市场分发

- 原子化服务免安装调用

- 多端协同自动发现

**Linux/macOS 客户端（Java 版）**

- Linux: \.deb/\.rpm/AppImage 包

- macOS: \.dmg 安装包，公证签名

- Java Agent JAR 可独立加载

**使用流程**

1. 下载安装 PACC 客户端

2. 注册反作弊账号（独立注册，与游戏账号无关）

3. 登录反作弊账号

4. 启动游戏（基岩版/Java 版自动识别），PACC 自动开始检测

5. 检测数据自动上报 PTV，红屏警告自动接收显示

## PTV 管控服务器部署

- **PTV 官方云服务（SaaS）**：直接使用 PTV 官方云端管控服务，开箱即用，按反作弊账号数/在线峰值计费。玩家端连接 PTV 官方 API 地址即可。**管理后台部署于 PTV 官方服务器**

- **私有化部署**：提供 Docker Compose / Kubernetes 部署方案，PTV 管控平台完全部署在客户自有服务器。适合对数据隐私有严格要求的企业客户。**管理后台部署于客户自有 PTV 服务器**

- **混合部署**：检测数据在客户本地 PTV 节点处理，AI 模型训练与特征库更新使用 PTV 官方云端服务

**游戏服务器零部署**：本架构**不需要在游戏服务器上安装任何组件**，不需要游戏服务器提供任何接口或数据，不需要游戏服务器运营方的配合。PACC 系统完全独立于游戏服务器运行。基岩版第三方服务器与 Java 版第三方服务器均无需任何改动。

# 性能与安全考量

## 性能指标

|指标|目标值|说明|
|---|---|---|
|玩家端 CPU 占用|\< 3%|Windows 客户端，游戏运行时|
|玩家端内存占用|\< 250MB|包含驱动、用户态服务、AI 模型、双端注入模块|
|红屏 overlay 渲染开销|\< 1% GPU|不影响游戏帧率|
|检测响应延迟|\< 50ms|从异常发生到 PTV 决策生成|
|红屏广播延迟|\< 3s|从 PTV 生成事件到 99% 在线玩家显示红屏|
|键盘锁定响应|\< 10ms|从红屏触发到键盘输入被过滤驱动拦截|
|特征库下发延迟|\< 10s|从 PTV 更新到全量玩家端生效|
|红屏误报率|\< 0\.005%|触发红屏锁定的误判比例（v4\.0 因加入查端复核，实际误报可撤销）|
|检测覆盖率|\> 95%|主流外挂类型的检测覆盖（基岩版 \+ Java 版）|
|玩家端崩溃率|\< 0\.1%|因反作弊导致的游戏崩溃概率|
|PTV 服务可用性|\> 99\.9%|月度可用性|
|PTV 并发广播能力|10 万\+|在线玩家同时推送红屏警告|
|查端并发能力|100\+|同时进行的远程查端会话数|

## 性能优化策略

- **异步检测**：玩家端所有检测逻辑在独立线程池（Java 21 虚拟线程）执行，不阻塞游戏主线程

- **分层检测**：玩家端先执行低成本规则检测 \+ 端侧 AI，异常玩家数据上报 PTV 再执行高成本 AI 检测

- **采样检测**：低风险账号降低检测频率，高风险账号提升检测频率

- **批量处理**：PTV AI 推理、数据上报采用批量处理，减少上下文切换

- **内存池**：玩家端使用对象池复用数据包、检测对象，减少 GC 压力

- **共享内存**：Java 与 Native 层通过 DirectByteBuffer / FFM API 共享内存，实现零开销数据交换

- **增量扫描**：内存扫描仅扫描变化页面，数据量为全量的 5\-10%

- **虚拟线程广播**：PTV 红屏广播使用 Java 21 虚拟线程，万级并发推送低开销

- **边缘缓存**：特征库/红屏策略在玩家端本地缓存，PTV 不可用时基础检测正常运行

- **SIMD 加速**：特征码匹配使用 Java 21 向量 API 实现 SIMD 加速

## 安全设计

- **通信加密**：全链路 TLS 1\.3 加密，玩家端与 PTV 双向证书认证（mTLS）

- **数据签名**：玩家端检测上报数据使用 HMAC\-SHA256 签名，防止篡改

- **玩家端保护**：Java 代码混淆 \+ 加密（ProGuard \+ 自定义加密 ClassLoader），内核驱动具备反调试、反 Hook、反卸载能力

- **防重放攻击**：通信协议包含时间戳和随机数，防止重放攻击

- **权限最小化**：PTV 数据库账号、API 密钥遵循最小权限原则

- **数据脱敏**：反作弊账号在红屏警告中脱敏显示，玩家隐私数据脱敏存储

- **抗逆向工程**：代码混淆、字符串加密、反编译保护、调试检测、虚拟化保护（VMProtect/Themida）

- **PTV 隔离**：PTV 管控平台与游戏服务器网络完全隔离，管理后台独立部署，避免游戏服务器被入侵影响管控系统

- **账号安全**：密码 Argon2id 加盐哈希存储，新设备登录验证，支持 TOTP 二次验证

- **查端安全**：远程查端通道 WebRTC \+ DTLS\-SRTP 加密，管理员 mTLS 认证，全程审计

- **证据完整性**：本地证据哈希链 \+ 云端双备份，防止证据篡改或删除

## 用户隐私保护

**数据采集范围**

- 最小必要原则：仅采集反作弊必需的本地数据

- 游戏内数据：仅采集与作弊检测相关的游戏状态与行为

- 系统数据：仅采集进程、设备、内存等系统级信息

- **不采集**：聊天内容、游戏账号信息、个人文件、浏览记录等隐私信息

- 不关联游戏账号：反作弊账号与游戏账号完全解耦

- 透明告知：明确告知用户采集的数据范围与用途（玩家协议）

**数据存储与用户控制权**

- 传输加密：TLS 1\.3 加密传输

- 存储加密：敏感数据 AES\-256 加密存储于 PTV

- 数据脱敏：红屏警告中账号脱敏显示

- 用户可随时卸载反作弊客户端

- 用户可申请注销反作弊账号、删除个人数据（红屏证据按法规保留）

- 用户可查看与导出自己的检测数据与记录

- 查端数据仅用于作弊判定，不用于其他用途

# 测试与质量保障

## 测试体系

|测试类型|测试内容与标准|
|---|---|
|单元测试|Java 层：JUnit 5 \+ Mockito，核心模块覆盖率 \> 80%；Native 层：Google Test；驱动层：虚拟机环境功能测试；CI 自动执行|
|集成测试|玩家端模块间接口测试、玩家端\-PTV 通信测试、PTV 微服务集成测试、红屏广播端到端测试、远程查端端到端测试、数据库集成测试|
|性能测试|玩家端 CPU/内存占用基准测试、内存扫描性能测试、网络通信压力测试（并发连接数、吞吐量）、PTV AI 模型推理性能测试、红屏广播万级并发压力测试、查端并发压力测试|
|对抗测试|已知作弊工具对抗测试（验证检测率，覆盖基岩版 \+ Java 版主流外挂）、绕过尝试测试（模拟作弊者绕过手法）、逆向工程测试（评估抗逆向强度）、对抗性样本测试（验证 AI 模型鲁棒性）|
|红屏警告测试|警告触发准确性测试（误报率验证）、广播延迟测试、万级玩家并发广播测试、显示效果测试（各平台/各分辨率/多显示器）、键盘锁定测试、冷却期与去重测试|
|远程查端测试|查端连接稳定性测试、取证数据完整性测试、查端权限控制测试、查端审计日志测试、断网重连测试、超时机制测试|
|跨平台测试|Windows 10/11、Android 8\-14、iOS 14\-17、iPadOS、HarmonyOS 3\-4、Linux（Ubuntu/Debian/Fedora）、macOS 12\+ 全平台兼容性测试|
|双端兼容测试|基岩版 1\.20\-1\.26 全版本、Java 版 1\.8\-1\.21 全版本、Forge/Fabric/Quilt 加载器兼容测试|

## 持续集成与部署

- **CI/CD 流水线**：代码提交 → 自动编译 → 单元测试 → 代码质量检查 → 每日构建 → 自动化部署 → 灰度发布

- **代码质量保障**：Checkstyle / SonarQube 规范检查、FindBugs / Coverity 静态分析、核心代码双人评审、定期安全审计与渗透测试

- **灰度发布**：玩家端新版本/特征库按 1% → 10% → 50% → 100% 逐步推送，监控关键指标（崩溃率、红屏误报率）

- **回滚机制**：异常时一键回滚到上一个稳定版本，特征库支持多版本并存快速切换

# 版本管理与兼容性

## 版本管理策略

- **语义化版本号**：主版本\.次版本\.修订版本（架构级变更 / 新增功能向下兼容 / Bug修复与特征库更新）

- **静默更新**：特征库、策略配置、AI 模型参数后台静默更新，用户无感知

- **增量更新**：玩家端客户端更新采用增量包，减少下载流量

- **强制更新**：安全相关关键更新强制升级

## 系统兼容性矩阵

### 操作系统支持

|操作系统|支持状态|说明|
|---|---|---|
|Windows 11 \(64位\)|完全支持|主力支持平台，完整功能（内核驱动\+全检测\+红屏显示\+键盘锁定\+查端）|
|Windows 10 \(64位\)|完全支持|完整功能支持|
|Windows 8\.1 \(64位\)|有限支持|基础功能，部分高级特性不可用|
|32位 Windows|不支持|仅支持 64 位系统|
|Android 8\.0\+|完全支持|Root/非Root双模式，基岩版为主|
|iOS 14\.0\+|完全支持|越狱/非越狱双模式|
|iPadOS 14\.0\+|完全支持|全屏/分屏适配，基岩版为主|
|HarmonyOS 3\.0\+|完全支持|原生 ArkTS 适配|
|Linux \(Ubuntu 20\.04\+/Debian 11\+/Fedora 34\+\)|完全支持|Java 版专用，eBPF 检测|
|macOS 12\+ \(Intel/Apple Silicon\)|完全支持|Java 版专用，Endpoint Security 检测|

### Minecraft 版本支持

- **基岩版**：支持 Minecraft Bedrock 1\.20\.x \~ 1\.26\.x 全系列版本（玩家端核心支持）

- **Java 版**：支持 Minecraft Java Edition 1\.8\.x \~ 1\.21\.x 全系列版本（v4\.0 新增）

- **加载器支持**：Java 版支持 Forge、Fabric、Quilt、LiteLoader，以及原版无加载器

- **游戏版本更新后 48 小时内完成玩家端适配**

- **保留最近 3 个大版本的完整支持，旧版本逐步停止支持前提前 30 天通知**

# 应急响应与运维保障

## 应急响应机制

### 事件分级

|级别|定义|响应时间|
|---|---|---|
|P0 紧急|大规模红屏误报、PTV 服务崩溃、玩家端严重安全漏洞、检测被大规模绕过、红屏广播系统故障、远程查端系统故障|15 分钟内响应，2 小时内出方案|
|P1 高|检测失效、PTV 服务降级、数据异常、新型外挂爆发|30 分钟内响应，4 小时内出方案|
|P2 中|功能异常、性能下降、部分玩家影响|2 小时内响应，24 小时内出方案|
|P3 低|界面问题、文案错误、次要功能异常|工作日 24 小时内响应|

### 0day 作弊应对

1. PTV AI 引擎或用户举报发现新型作弊手段或 0day 漏洞

2. 紧急分析作弊原理，从玩家端采集的样本中提取特征

3. 快速更新特征库，PTV 热更新下发到全量玩家端，10 秒内生效

4. 评估影响范围，在 PTV 管理后台发布安全公告

5. 后续版本从架构层面修复漏洞

### 红屏误报应急处理

1. PTV 监控到大规模红屏误报报告或人工审核发现误判趋势后立即启动应急响应

2. PTV 紧急暂停相关检测规则/特征库下发，防止更多误报

3. 对已锁定的误报玩家，管理员批量执行查端并解除限制

4. 快速排查原因，定位问题（特征库误报/AI 模型偏差/阈值设置不当）

5. 批量删除受影响的误报记录，在管理后台标记为"误报已撤销"，恢复信誉评分

6. 向受影响账号发送致歉通知（通过 PTV 客户端消息）

7. 复盘总结，优化检测规则与测试流程

## 运维监控体系

- **系统监控指标**：玩家端指标（在线率、崩溃率、检测次数、资源占用）、PTV 服务端指标（QPS、响应时间、错误率、WebSocket 连接数）、红屏广播指标（广播次数、送达率、平均延迟）、查端指标（队列长度、平均查端时长、并发数）、数据库指标（连接数、慢查询、吞吐量、存储空间）、业务指标（日活、检测率、红屏误报率、账号注册量）

- **告警机制**：多级告警（信息→警告→严重→紧急）、多渠道通知（邮件、企业微信、短信、电话）、告警收敛（避免告警风暴）、7×24 小时值班制度

- **日志系统**：ELK / Loki 日志栈、日志分级（DEBUG/INFO/WARN/ERROR/FATAL）、审计日志（所有 PTV 管理操作与查端操作留痕）、热数据 7 天 / 温数据 30 天 / 冷数据 1 年

## 容灾与高可用

- **PTV 服务高可用**：多可用区部署，单机房故障不影响服务；无状态服务设计，水平扩展；负载均衡 \+ 健康检查，自动摘除故障节点；降级机制（高峰期自动降级非核心功能，优先保障红屏广播与检测数据上报与查端）

- **数据备份**：数据库每日全量备份 \+ 实时增量备份；备份数据异地存储，跨机房同步；定期演练恢复流程；核心数据（账号、红屏记录、证据、查端记录）永久

- **玩家端离线模式**：PTV 服务不可用时，玩家端切换到离线模式，本地缓存的特征库/检测规则继续生效，基础检测功能正常运行；检测数据本地缓存，服务恢复后自动上报；离线模式最长可用 72 小时；离线模式下不触发红屏广播（需 PTV 决策），但本地证据持续记录

- **红屏广播容灾**：红屏警告服务多实例部署，单实例故障不影响广播；广播消息持久化到 Redis，故障恢复后未送达消息可补发（仅限在线玩家）

- **查端容灾**：查端服务多实例部署，查端会话状态持久化，故障恢复后可继续未完成的查端

# 附录

## 支持的外挂类型与检测方式全清单

|类别|外挂名称|检测方式（玩家端本地采集 \+ PTV AI 分析）|
|---|---|---|
|底层作弊类|内存修改（X\-Ray/飞行/速度/无敌）|玩家端驱动级内存扫描 \+ 页保护 \+ 特征码匹配（双端）|
||进程注入（DLL 注入/反射注入/Java Agent注入）|玩家端内核回调 \+ 模块加载监控 \+ JVM类加载监控 \+ 注入路径检测|
||调试工具（Cheat Engine/x64dbg/JDWP）|玩家端进程/窗口/驱动/调试寄存器/VEH/JVMTI多维度检测|
||硬件外挂（硬件宏/USB 注入器/Arduino）|玩家端 USB 设备 DNA 画像 \+ HID 描述符分析 \+ 输入时序分析|
|输入作弊类|AutoClicker（自动点击器）|玩家端 IRP 拦截 \+ 点击间隔方差 \+ 熵值分析 \+ PTV AI 模式识别|
||TriggerBot（触发 bot）|玩家端游戏行为采集 \+ PTV 反应时间分析 \+ 命中精度分析|
||Aimbot（自瞄）|玩家端视角/鼠标轨迹采集 \+ PTV 旋转速度分析 \+ DTW 轨迹匹配|
||宏脚本（鼠标/键盘宏）|玩家端输入时序分析 \+ 轨迹运动学特征 \+ PTV 行为模式识别|
|游戏行为作弊类|KillAura（杀戮光环）|玩家端战斗行为采集 \+ PTV 角度/频率/目标切换多维度分析|
||Reach（远程攻击）|玩家端攻击距离数据采集 \+ PTV 精确距离计算 \+ 延迟补偿|
||Fly/Speed/NoFall（移动类）|玩家端移动行为采集 \+ PTV 重力模拟 \+ 速度阈值 \+ 加速度检测|
||Scaffold/AutoTotem/CrystalAura（交互类）|玩家端交互行为采集 \+ PTV 放置精度/切换速度/同步性分析|
||Baritone（自动行走/路径规划）|玩家端移动轨迹采集 \+ PTV 路径规划特征识别 \+ 包名检测|
|基岩版客户端类|作弊客户端（Horion/Borion/Axon/FPI等）|玩家端内存特征码 \+ 模块检测 \+ 进程/窗口检测|
||幽灵客户端（Ghost Client/Undisputed）|玩家端内存/线程隐藏检测 \+ PTV AI 行为模式识别（无进程特征时）|
||资源包/行为包作弊（X\-Ray 包等）|玩家端本地文件完整性校验 \+ PTV 违规资源包特征库匹配|
|Java版客户端类|通用作弊客户端（Wurst/Impact/Sigma/Meteor等）|玩家端 JVM 类加载检测 \+ 包名特征 \+ Mod清单扫描 \+ 字节码特征|
||付费作弊客户端（Future/RusherHack/Pyro/Gamesense）|玩家端加载器检测 \+ native库检测 \+ 网络验证特征 \+ 内存特征|
||幽灵客户端（Raven B\+/NightX/Skidder）|玩家端类加载监控 \+ 反射调用分析 \+ PTV AI 行为模式识别|
||协议外挂/代理作弊|玩家端本地网络包采集 \+ 异常数据包检测 \+ 代理进程检测|

## 技术栈与编程语言全清单

**说明**：PACC v4\.0 以 **Java 21 LTS** 为主要开发语言，以下为全系统可能用到的所有编程语言与技术的完整清单，按类别单独列出。

|类别|语言/技术|用途|
|---|---|---|
|核心开发语言|Java 21 LTS|主要语言。玩家端用户态服务、Java版注入层（Java Agent/ASM）、PTV 云端微服务（Spring Boot 3\.x）、AI 推理调度、红屏广播、远程查端服务|
||C / C\+\+|Windows 内核驱动（WDF）、Native 检测引擎、基岩版 DLL 注入模块、Android NDK、iOS Native 层、Linux/macOS Native 层|
||Rust|安全敏感模块、驱动组件、高性能工具链、本地证据存储引擎、内存安全要求高的组件|
|移动端开发|Kotlin|Android 客户端主语言|
||Swift|iOS/iPadOS 客户端主语言|
||Objective\-C|iOS 系统 API 桥接、C 库互操作|
||ArkTS|HarmonyOS 客户端主语言|
|后端与基础设施|Go|PTV 高性能 API 网关、红屏广播推送服务、数据管道、日志采集|
||Python|PTV AI 模型训练（PyTorch/TensorFlow）、数据分析、特征工程、自动化测试脚本|
||SQL|MySQL / ClickHouse / PostgreSQL 数据查询与存储过程|
|前端开发|TypeScript / React|PTV 管理后台、查端控制台、数据可视化（ECharts）|
|脚本与运维|PowerShell|Windows 玩家端部署脚本、安装包脚本、运维自动化|
||Bash / Shell|Linux PTV 运维、CI/CD 流水线、Docker 构建|
||Groovy|Gradle 构建脚本、Jenkins CI 流水线|
|数据格式与协议|Protocol Buffers|玩家端\-PTV 通信协议序列化、查端指令协议|
||JSON / YAML|配置文件、API 数据、Mod 清单解析（fabric\.mod\.json）|
||TOML|玩家端本地配置、Forge mods\.toml 解析|
|安全敏感与扩展|C\#|Windows 桌面管理工具、安装程序 GUI（WPF/WinForms）|
||Lua|动态规则引擎、热更新逻辑脚本、检测规则 DSL|
||WebAssembly \(WAT/WASM\)|端侧轻量检测模块、沙箱化规则执行|
||eBPF \(C\)|Linux 平台系统调用监控、网络包捕获、进程追踪|
|Java 版专项技术|Java Agent / Instrumentation API|Java 版注入、类转换、字节码增强|
||ASM / ByteBuddy|Java 字节码操作、行为采集点植入|
||JVMTI \(C/C\+\+\)|JVM 层深度监控、类加载钩子、字段访问回调|
||Gradle \(Groovy/Kotlin DSL\)|Java 版模块构建、依赖管理|
|前端与可视化扩展|HTML5 / CSS3|管理后台界面、查端控制台界面|
||WebRTC \(JavaScript\)|远程查端实时音视频/数据通道|
||GraphQL|管理后台 API 查询语言（可选）|
|数据库与中间件|MySQL SQL|关系型数据存储（账号、记录、特征库）|
||ClickHouse SQL|时序检测日志、行为特征数据|
||Redis \(RESP\)|缓存、在线状态、广播消息队列|
|AI 与机器学习|Python \(PyTorch\)|AI 模型训练、XGBoost、LSTM\-AE、用户嵌入|
||ONNX|端侧 AI 模型格式、跨平台推理|

## 开源许可证

本项目采用 **GNU Affero General Public License v3\.0 \(AGPLv3\)** 开源许可证。

AGPLv3 是强 Copyleft 许可证，核心要求：

- 修改或衍生作品必须同样以 AGPLv3 开源

- 通过网络提供服务时，必须向用户提供完整源码

- 保留版权声明与许可证文本

- 禁止硬件锁死（DRM）限制用户运行修改后的程序

## 术语表

|术语|定义|
|---|---|
|PACC|Professional Anti\-Cheat Client，专业反作弊客户端，本系统名称|
|PTV|PotatoTV，管控服务器提供方，管理后台部署于此，非游戏服务器|
|红屏警告|PACC 唯一的作弊响应手段，全屏红色覆盖 \+ 强制暂停键盘 \+ 广播公示|
|远程查端|管理员通过加密通道远程连接被锁定玩家端进行深度取证的过程|
|反作弊账号|玩家单独注册的 PACC 账号，与游戏账号完全解耦|
|基岩版|Minecraft Bedrock Edition，C\+\+ 开发的跨平台版本|
|Java 版|Minecraft Java Edition，Java 开发的 PC 版本|
|CE|Cheat Engine，常用内存修改工具|
|A端|A 端作弊客户端，基岩版常见作弊工具类别|
|设备 DNA|基于 USB 设备硬件特征生成的唯一标识，用于跨账号关联分析|
|证据哈希链|每条证据记录包含前一条记录的哈希，形成不可篡改的链式结构|

> （注：部分内容可能由 AI 生成）
