apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pacc.fullname" . }}-frontend
  labels:
    app: pacc-frontend
    {{- include "pacc.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount.frontend }}
  selector:
    matchLabels: { app: pacc-frontend }
  template:
    metadata:
      labels: { app: pacc-frontend }
    spec:
      containers:
        - name: frontend
          image: {{ include "pacc.image" (dict "image" .Values.images.frontend) }}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          resources:
            {{- toYaml .Values.resources.frontend | nindent 12 }}
          readinessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pacc.fullname" . }}-frontend
  labels:
    app: pacc-frontend
    {{- include "pacc.labels" . | nindent 4 }}
spec:
  selector: { app: pacc-frontend }
  ports:
    - name: http
      port: 80
      targetPort: http