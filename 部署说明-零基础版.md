# PACC 反作弊系统 · 零基础部署说明

> 本说明面向**完全没有电脑基础**的读者，一步一步照着做即可。
> 全文只说"怎么做"，不说"为什么"。照做即可成功。

---

## 一、这份说明是干什么的？

它是教你把 **PACC 反作弊系统** 装到一台电脑 / 服务器上运行起来的说明书。

系统装好之后，你会得到：

| 你能用到的东西 | 访问地址 |
|---|---|
| **管理后台**（管玩家、看作弊记录、管赛事） | `http://你的电脑地址:8081` |
| 后端接口（一般不需要你碰） | `http://你的电脑地址:8080` |
| 数据库（存数据的仓库，自动建好） | 在 Docker 里，见下文"数据库说明" |

---

## 二、你需要提前准备的东西

### 1. 一台电脑或云服务器

| 项目 | 最低要求 | 建议 |
|---|---|---|
| 操作系统 | Windows 10 / 11 64 位 | Windows 11 |
| 内存（内存条容量） | 8 GB | 16 GB 以上 |
| 硬盘空余空间 | 20 GB | 越多越好 |
| 网络 | 能上网 | 稳定宽带 |

> 电脑越新越好。如果这台电脑是**专门跑服务**的（比如云服务器），建议买 2 核 4G 以上的配置。

### 2. 需要安装的一个软件：Docker Desktop（翻译：码箱子管理工具）

Docker 是一个"把整个系统打包成一箱箱货，一键拆箱就能跑"的工具。你只需要装一个它，剩下所有软件（数据库、后端、前端）它都会自动帮你装好。

> ⚠️ 本文所有操作**默认在 Windows 系统**演示。如果你用的是云服务器（Linux），命令基本一样，只是第 3 步安装方式不同（见下方小字）。

---

## 三、第 1 步：安装 Docker Desktop

1. 打开浏览器（就是上网那个图标，一般是蓝色地球）。
2. 在地址栏输入：`https://www.docker.com/products/docker-desktop/` 然后按回车。
3. 找到写着 **Download for Windows**（或"下载 Windows 版"）的大按钮，点它。
4. 下载完成后，双击刚下载的文件（文件名类似 `Docker Desktop Installer.exe`）。
5. 安装过程中弹出的窗口，**全部直接点"下一步 / Next / 同意 / OK"**，不要改任何设置。
6. 安装完成后，**重启电脑**（点"开始"→"电源"→"重启"）。
7. 重启后，在桌面或开始菜单找到 **Docker Desktop** 图标，双击打开它。
8. 第一次打开会问你是否同意条款，点"同意 / Accept"即可。
9. 等待左下角 / 鲸鱼图标变成 **绿色**（变成绿色 = 已经就绪）。可能需要几分钟，第一次启动慢很正常。

> **小提示**：如果系统弹出提示"需要启用 WSL2"或"需要虚拟化"，通常点"确定/OK"让它自动处理即可。若报错，翻到文末"常见问题"看解决办法。

---

## 四、第 2 步：把项目文件夹放到电脑里

系统代码在一个文件夹里（就是你看到的这份文档所在的文件夹，包含 `ptv-backend`、`ptv-frontend`、`docker-compose.yml` 等）。

- 如果你是从别人那里拿到的**整个文件夹**：把它放到一个好找的位置，例如 `D:\pacc`（建议不要放在桌面，防止误删）。
- 如果是压缩包（`.zip` 等）：先右键 →"解压到当前文件夹"。
- 如果是代码仓库（Git）：需要在电脑上安装 Git 后执行 `git clone <仓库地址>`；**没有代码仓库的话请忽略这句**。

**确认位置**：文件夹里应该能看到一个叫 `docker-compose.yml` 的文件（后面所有操作都要在这个文件夹里做）。

> 后面凡是说"打开项目文件夹"，都是指这个文件夹。**建议给它在桌面建一个快捷方式**，方便找。

---

## 五、第 3 步：准备配置文件（.env，用来填密码和密钥）

系统启动前，需要一份"设置文件"告诉它密码是什么。这份文件叫 `.env`。

1. 打开"项目文件夹"。
2. 找到文件 `env.example`（名字是 `.env.example`，如果看不到，见文末"常见问题"第 3 条）。
3. **复制一份**这个文件：
   - 右键 `env.example` →"复制"
   - 在文件夹空白处右键 →"粘贴"
   - 这时会出现一个"`env.example - 副本`"文件
4. 把它**改名为** `.env`：
   - 右键"`env.example - 副本`" →"重命名"
   - 把名字改成 `.env`（注意：`.` 在最前面，`env` 后面没有任何后缀），回车。
   - 如果弹出"更改扩展名可能无法使用"的提示，点"是 / Yes"。

5. 用**记事本**打开 `.env`（右键 →"打开方式"→"记事本"）。

6. 你只需要**重点改下面几个**（把 `=` 后面的内容换成你自己的密码）：

```
MYSQL_ROOT_PASSWORD=pacc-root-change-me        ← 数据库超级密码（建议改）
MYSQL_PASSWORD=pacc-secret-change-me           ← 数据库普通密码（建议改）
PACC_ADMIN_API_KEY=pacc-admin-secret-key       ← 管理后台登录密钥（务必改）
PACC_JWT_SECRET=PACC-v4-runtime-secret-key-change-me-in-production-0123456789   ← 令牌密钥（务必改）
```

> 密码规则：建议用**英文大小写 + 数字**组合，比如 `MyPass2026!abc`。别用纯数字，容易被猜。
> 改完后**保存**（Ctrl + S），然后关掉记事本。

7. 如果你想**带演示数据**（推荐，方便看效果），保持文件里 `PACC_SEED=true` 这一行不变即可。

---

## 六、第 4 步：启动系统（关键步骤）

1. 打开 **PowerShell**（一个黑色窗口，输入命令用）：
   - 点击"开始"菜单 → 输入 `PowerShell` → 回车；或
   - 在**项目文件夹**地址栏输入 `powershell` 后回车（推荐，会自动进入项目文件夹）。
2. 如果你是用"开始菜单"打开的 PowerShell，需要先"走进"项目文件夹。输入下面这行后回车（把路径换成你的实际位置）：

```powershell
cd D:\pacc
```

3. 输入下面这行，然后回车，开始启动（第一次会下载很多东西，可能需要 **10～30 分钟**，耐心等待，不要关窗口）：

```powershell
docker compose up -d --build
```

4. 等命令**执行完**（回到可以输入命令的状态，没有报红字错误）就说明启动完成。

> **怎么判断"执行完了"**：画面停下来，又出现一个光标在闪烁，可以继续打字 = 执行完了。

---

## 七、第 5 步：检查系统是否启动成功

输入下面这行，回车：

```powershell
docker compose ps
```

你会看到一张表格，里面有一列叫 **STATUS**（状态）。**重点看**这几行：

| 容器 | 看到什么算正常 |
|---|---|
| pacc-mysql | `healthy` |
| pacc-backend | `healthy` |
| pacc-frontend | `healthy` |
| pacc-gateway | `Up`（或 healthy） |

- 如果全是 `healthy` / `Up`：**成功！** 进入第 6 步。
- 如果有的写着 `starting` / `unhealthy` / `restarting`：**别慌**，等 1～2 分钟再输入一次 `docker compose ps` 看一遍。
- 如果出现 `Exited`（已退出）或 `Error`：翻到文末"常见问题"。

---

## 八、第 6 步：打开管理后台

1. 打开浏览器。
2. 在地址栏输入：

```
http://localhost:8081
```

3. 按下回车，应该能看到登录页面。
4. 输入你在 `.env` 里设置的 **PACC_ADMIN_API_KEY**（没改的话就是默认的 `pacc-admin-secret-key`）。
5. 点击登录，进入后台。

> 成功标志：能看到左侧菜单（数据大盘、红屏管理、账号、赛事风控、赛事进程……）。
> 如果你在 `.env` 里保留了 `PACC_SEED=true`，登录后"账号"页能查到演示账号，赛事页也有演示数据。

---

## 九、数据库说明（自动建好 + 手动建库 SQL 参考）

### 1. 你需要手动建数据库吗？

**不需要。** 系统在启动时（第 4 步）会**自动**完成这些事情：

1. 自动创建数据库（名字默认 `pacc`）；
2. 自动创建数据库账号和密码（就是你在 `.env` 里填的那两个）；
3. 后端启动时**自动建好所有数据表**（不需要写任何建表语句）。

你唯一要做的，就是在 `.env` 里把密码填好。**如果你完全不想碰数据库，直接跳过本段，不影响部署。**

### 2. 系统会自动创建的数据库表（知道一下即可）

系统会自动创建下面这些表（表格名已经过统一处理，实际会在这些词前面加上系统前缀，例如 `account`、`device_record`……）：

| 分类 | 表（自动创建） | 存的什么 |
|---|---|---|
| 账号 | `account` | 玩家反作弊账号（PTEID、邮箱、信誉分） |
| 设备 | `device_record`、`peripheral` | 玩家设备指纹、外设 |
| 登录 | `login_event` | 每次登录记录（IP、设备） |
| 检测 | `detection_event`、`cheat_record`、`feature_vector`、`cheat_type` | 检测事件、作弊记录、特征向量 |
| 红屏 | `redscreen_alert` | 红屏警告记录 |
| 查端 | `inspect_session` | 管理员远程查端会话 |
| 特征库 | `signature` | 作弊特征签名 |
| 赛事 | `enrollment`、`match_session`、`suspicion_flag`、`tournament_stage`、`tournament_notice`、`tournament_config` | 报名、对局、嫌疑、赛程、公告、配置 |
| 客服 | `support_ticket`、`appeal` | 客服工单、申诉 |

### 3. 如果偏要自己先建库（可选 · 懂的人看）

如果你用的是**外部 MySQL**（而不是 Docker 里那个自动建好的），可以先用下面的 SQL 把"数据库 + 账号"建出来，表仍然由系统自动创建：

```sql
-- 1) 创建数据库（utf8mb4 支持中文，建议）
CREATE DATABASE IF NOT EXISTS pacc
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- 2) 创建专用账号（把密码换成你在 .env 里设置的 MYSQL_PASSWORD）
CREATE USER IF NOT EXISTS 'pacc'@'%' IDENTIFIED BY 'pacc-secret-change-me';

-- 3) 给账号授权（只给这一个库的权限，最小权限原则）
GRANT ALL PRIVILEGES ON pacc.* TO 'pacc'@'%';
FLUSH PRIVILEGES;
```

> 然后把 `.env` 里的数据库地址指向你自己的 MySQL（这部分属于高级用法，0 基础读者直接跳过）。

### 4. 内置的开发数据库 H2

系统默认（没有 Docker 时）自带一个叫 **H2** 的"演示数据库"，**只在本地开发用**，生产 / Docker 部署一律用上面的 **MySQL**。两者不用你手动区分，Docker 启动时会自动用 MySQL。

---

## 十、常用命令速查（抄下来备用）

| 我要做什么 | 在 PowerShell 输入（在项目文件夹里） |
|---|---|
| 查看是否启动成功 | `docker compose ps` |
| 启动系统 | `docker compose up -d --build` |
| 停止系统（数据保留） | `docker compose down` |
| 停止并清空数据（慎用） | `docker compose down -v` |
| 看后端日志 | `docker compose logs -f ptv-backend` |
| 看前端日志 | `docker compose logs -f ptv-frontend` |
| 看数据库日志 | `docker compose logs -f mysql` |
| 重启某个部分 | `docker compose restart ptv-backend` |
| 进入数据库（命令行） | `docker exec -it pacc-mysql mysql -upacc -p` |

---

## 十一、常见问题（遇到报错先看这里）

### 1. Docker Desktop 打不开 / 报 WSL 错误

- 重新启动电脑后再打开 Docker Desktop。
- 检查"Windows 功能"：开始菜单搜"启用或关闭 Windows 功能"→ 勾选 **"适用于 Linux 的 Windows 子系统 (WSL2)"** 和 **"虚拟机平台"** → 确定 → 重启电脑。
- 还不行就重装一遍 Docker Desktop。

### 2. `docker compose` 报"不是内部或外部命令"

说明 Docker 没装好或没启动。确认 Docker Desktop 右下角图标是**绿色**后再试。

### 3. 看不到 `.env.example` 文件

Windows 默认隐藏"扩展名"。在项目文件夹点"查看"→ 勾选 **"文件扩展名"**，就能看到了。

### 4. 启动时报"端口被占用"（端口是 8080 / 8081 / 3306 等）

说明电脑上已有程序占用这些端口。两个办法选一个：
- 关掉占用端口的程序（多半是你之前装过的软件）；
- 或改端口：编辑 `.env`，把 `PACC_BACKEND_PORT`、`PACC_FRONTEND_PORT` 改成别的数字（如 `8082`），再重新 `docker compose up -d --build`。

### 5. 页面打开了但登录失败

- 确认登录框里输入的密钥 = `.env` 里的 `PACC_ADMIN_API_KEY`（复制粘贴，别手打）。
- 改过 `.env` 之后需要重启：`docker compose up -d --build`（改了 `.env` 一定要重新执行这条）。

### 6. 改了 `.env` 后没生效

`.env` 修改后**必须**重新执行 `docker compose up -d --build`，否则不生效。

### 7. 手机 / 别人访问不了

- 确认访问的是 `http://电脑的局域网IP:8081`（不是 `localhost`）。
- 局域网 IP 查看：`开始`→ 输入 `cmd` 回车 → 输入 `ipconfig` 回车 → 看"IPv4 地址"那一行。
- 需要把 8080、8081 等端口在防火墙 / 云服务器安全组里"放行"（这属于服务器设置，云服务器在控制台操作）。

---

## 十二、高级：配置域名和 HTTPS（可选，给有域名的用户）

默认直接用 IP / localhost 就能用。如果你有自己的域名（比如 `potatotv.asia`），可以做成更正规的网址：

1. 在域名服务商后台，把下面 4 个子域名解析到你的服务器公网 IP：
   - `admin.potatotv.asia` → 管理后台
   - `api.potatotv.asia` → 后端接口
   - `pacc.potatotv.asia` → 玩家端
   - `dl.potatotv.asia` → 下载站
2. 申请 HTTPS 证书（推荐免费的 Let's Encrypt，方法见 `deploy/gateway/certs/README.md`）。
3. 把证书文件 `potatotv.asia.crt` 和 `potatotv.asia.key` 放进 `deploy/gateway/certs/` 目录。
4. 重新构建网关：`docker compose up -d --build gateway`。

> 0 基础读者**跳过本段**不影响使用。

---

## 十三、附录：`.env` 配置项对照表

| 配置项 | 用途 | 部署时建议 |
|---|---|---|
| `MYSQL_ROOT_PASSWORD` | 数据库超级管理员密码 | 改成强密码 |
| `MYSQL_DATABASE` | 数据库名 | 一般不动，默认 `pacc` |
| `MYSQL_USER` / `MYSQL_PASSWORD` | 系统使用的数据库账号 / 密码 | 改成强密码 |
| `PACC_ADMIN_API_KEY` | **管理后台登录密钥** | **必须改** |
| `PACC_JWT_SECRET` | 玩家令牌签名密钥 | **必须改**（长度 ≥32 字符） |
| `PACC_SEED` | 是否写入演示数据（true / false） | 演示用 `true`，正式用 `false` |
| `PACC_BACKEND_PORT` | 后端端口 | 默认 8080，冲突可改 |
| `PACC_FRONTEND_PORT` | 管理后台端口 | 默认 8081，冲突可改 |
| `PACC_MYSQL_PORT` | 数据库对外端口 | 默认 3306 |
| `PACC_GATEWAY_HTTP_PORT` / `PACC_GATEWAY_HTTPS_PORT` | 网关 80 / 443 | 有域名时用 |

**启动成功标准（复习）**：`docker compose ps` 显示 mysql / backend / frontend 都是 `healthy`，浏览器打开 `http://localhost:8081` 能登录进入后台。

---

*如有其他问题，请把屏幕上红色的报错文字截图发给技术人员。*
