{
  "name": "pacc-desktop",
  "version": "5.0.0",
  "private": true,
  "description": "PACC v5.0 桌面端客户端壳（Tauri + 复用 ptv-frontend 构建产物）",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "web:build": "npm run build --prefix ../ptv-frontend"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}