{
  "name": "pacc-desktop",
  "version": "4.2.0",
  "private": true,
  "description": "PACC v4.2 桌面端客户端壳（Tauri + 复用 ptv-frontend 构建产物）",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "web:build": "npm run build --prefix ../ptv-frontend"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}