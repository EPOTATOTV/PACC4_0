# PACC 更新日志

版本号格式 `<major>.<minor>.<patch>`。内部代号 DF（Deep Fortress）指内核级防护与纵深防御这条主线，
与检测引擎的版本号解耦——引擎仍是 5.4.0，协议版本仍是 v1。

从 v5.4.0 起，发布件由 `scripts/build-all.sh` 一条命令产出，并在 `dist/<version>/SHA256SUMS.txt`
给出全部产物的校验和。签名走 `scripts/sign-artifacts.sh`。

---

## [5.4.0] - DF Alpha 1.0.0 - 2026-09-28

首个对外发布版本。功能基本完整、可接受早期用户测试，但仍在快速迭代，不承诺 SLA，
版本升级时演示数据可能被重置。

### 新增

- **APM 全链路性能监控**：端侧采集 54 项指标，服务端小时级聚合，超阈值自动告警，管理端可视化。链路是端侧采集 → 服务端聚合 → 管理端展示，不做采样近似。
- **客户端五层防篡改**：静态校验、运行时校验、内存校验、内核层校验、远程证明。逐层独立，任一层失守不会让其余层直接失效。
- **多维反调试**：10 个维度交叉验证（PEB / 硬件断点 / 时间差 / 异常分发 / 内核态调试标志等），单维度被绕过不足以隐藏调试行为。
- **反 Hook 检测**：覆盖 IAT、Inline、VTable、SSDT 等 6 类 Hook。
- **进程保护**：防终止、防注入、防 dump、防挂起，走内核对象回调裁剪高危句柄权限。
- **远程证明**：服务端挑战 - 响应，客户端侧无法伪造通过结果。
- **密钥管理系统**：根密钥经 HKDF 派生子密钥，90 天轮换周期，支持硬件安全存储。
- **安全事件哈希链**：审计日志逐条链式哈希，改动任意一条即断链，可检出篡改。
- **管理端新增 3 页**：APM 监控、安全审计、密钥管理。
- **Windows GUI 新增**：HookDetector、ProcessProtector、多维 DebugGuard。

### 改进

- 检测引擎 v5.2 → v5.4。
- 178 维特征采集补齐，特征回填率 15% → 85%。
- 暴力检测从单阈值判定改为时序 + 分布双模型，连点器检出率 80% → 95%。
- 全仓库版本号收敛到 5.4.0 一个值，并由 `scripts/bump-version.sh` 统一维护，
  避免 pom / package.json / csproj / gradle / Cargo.toml 各写一份互相漂移。
- 新增一键构建流水线 `scripts/build-all.sh`，产出、校验和、签名各自成脚本，步骤失败即中断。
- 下载站版本号改为单一来源 `deploy/dl-web/js/config.js`，页面徽标从它取值。
- 发布工程化补齐：CHANGELOG、升级指南、回滚方案、签名脚本。

### 修复

- 红屏后键盘锁定状态不恢复的问题。
- 查端回放视频在部分播放器上无法播放。
- 模型下发时大文件下载中断。
- 管理端在低分辨率下布局错乱。
- 插件热加载的路径边界收紧：`path` 只接受插件目录内的相对路径，绝对路径、越界路径与摘要不符的包一律拒绝；
  插件初始化与元数据读取也纳入沙箱的超时与 CPU 记账，不再在调用方线程裸跑。
- 发行 JAR 残留原始业务包名：ProGuard 的 `-keepdirectories` 会把空目录条目写回产物，
  改为一律不保留目录条目。

### 已知问题

- Linux 原生客户端尚未交付，Linux 用户暂用 Java 版；eBPF 与 procfs 降级路径已就绪，安装包未随本次发布。
- 内核层防护仍在开发：Windows 驱动源码与 Linux eBPF 程序已就位，尚未完成 WHQL 认证。
- 代码混淆只覆盖 Java 客户端与 C# GUI，Rust 侧未启用。
- HarmonyOS 端检测能力较 Windows 端弱约 40%。

### 升级注意

- 数据库需执行 V26–V34 迁移脚本，Flyway 启动时自动执行。
- 配置文件新增 v5.4 相关配置项，见 [docs/UPGRADE.md](docs/UPGRADE.md)。
- 旧版客户端与 v5.4 后端不兼容，下载站的 `min_version` 已抬到 5.4.0，旧端会被强制升级提示拦下。
- 本次发布后回滚需恢复数据库备份，迁移脚本不回退。

---

## [5.3.0] - 2026-09-25

### 新增

- 管理端只读聚合接口（设备、行为趋势、信誉总览），管理端页面从写死数据改为取真实聚合值。
- 查端回放落盘：环形录制 + 水印加密，保留 30 天。
- 回放逐帧在线预览，前端用 blob URL 并在卸载时释放，避免内存堆积。
- 硬件指纹增强与设备登记链路。
- 威胁情报自动提取，申诉 AI 自动复核。

### 改进

- 管理端补齐 5 个页面并接上 v5.3 只读聚合数据。

---

## [5.2.0] - 2026-09-25

### 新增

- 模型训练流水线、行为画像、信誉体系 v2。
- 模型下发链路：后端产物端点 + 端侧同步安装。
- Java 探针补齐模组验签、装载审计与字节码完整性校验。

### 改进

- 端侧 AI 从演示骨架换成真实推理。
- 特征采集补齐到 178 维；暴力检测换用时序与轨迹模型。

---

## [5.1.0] - 2026-09-24

### 新增

- 客户端与桌面壳加固：代码混淆、完整性探针、会话密钥。
- 移动端与 Android 探针加固，Rust 管道依赖锁补齐。

### 改进

- CI 上传测试报告与覆盖率，新增 CodeQL；构建缓存移出版本控制。
- 管理端接入下载趋势图表、变更历史与路由进度条。
- 下载站升级：系统识别推荐、多语言、主题切换、法务页、校验和展示。
- 下载计数按 IP 日配额限流；新增天 × 平台趋势与动效配置变更审计。

---

## [5.0.0] - 2026-09-12

### 新增

- PACC v5.0 P0 版本功能开发完成。
- 移动端（Capacitor）与桌面端（Tauri）壳。

### 改进

- 地图 BP 与直播转播功能。
- 管理端登录加固；WSS 会话密钥。

### 修复

- 对齐实体映射与 Flyway 迁移，修复全新 MySQL 库建库即失败。

---

## [4.8.0] - 2026-09-06

### 新增

- 租户管理、开放 API、BI 报表能力。

---

## [4.7.0] - 2026-09-06

### 新增

- 情报运营中台模块。
- 零日发现回流，闭合检测 - 情报链路。

### 改进

- 签名库增加状态分面筛选。

---

## [4.6.0] - 2026-09-06

### 新增

- 威胁情报与零日检测能力；威胁提升、聚类与分析。

---

## [4.5.0] / [4.4.0] - 2026-09-04

### 新增

- 全量功能更新，补齐 v4.4–v4.5 版本内容。

---

## [4.3.0] - 2026-09-03

### 新增

- 远程查端屏幕共享与分布式存储。

### 改进

- 全站品牌视觉升级；工单中心。

---

## [4.2.0] - 2026-09-04 至 2026-09-11

### 新增

- 跨平台检测能力；管理端页面补齐；桌面壳与移动壳脚手架。

### 改进

- 全站 UI 重构。

### 修复

- 插件审核事务与 SDK 签名细节。
- 登录流程超时错误处理。

---

## [4.1.0] - 2026-09-02

### 新增

- 远程查端、验证码、本地加密存储。

### 改进

- 认证迁移到 HttpOnly cookie，移除本地存储的密钥。

---

## [4.0.0] - 2026-08-31

### 新增

- 首个正式版：完整客户端部署与自动更新能力。
- 赛事反作弊系统与玩家门户。

### 改进

- 整合安全加固与全量功能迭代。